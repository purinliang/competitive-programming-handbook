# 课程结构整改执行计划

> 制定日期：2026-08-16
>
> 本文把 [课程结构](curriculum.md) 转成逐篇可执行清单。
> 最新决策是需求来源，本文是实施说明；旧文件、旧编号和旧单元只提供素材，不能
> 反过来限制课程结构。

## 整改原则

1. 先确定单元要完成的学习任务，再决定复用、收窄、拆分或新建哪些文章。
2. 一个章节可以超过十个单元。3–8 篇只是检查单元是否聚焦的经验值，不是合并
   不同主题的理由。
3. 已审阅或定稿正文中的正确讲解继续保留，但状态不保护错误的篇章边界。拆分后，
   原语义文件保留最接近标题的核心职责，新主题使用新的语义文件。
4. 字面量跟随对应类型第一次出现；变量和输入输出先给最小用法。常量在数组容量
   第一次需要固定编译期值时引入。这是螺旋式教学，不要求第一篇程序先解释完整
   语言标准。
5. 正文不维护“上一篇”“下一篇”。需要依赖其他知识时直接写文章名和真实关系。
6. 学习路线按单元组织教学任务；模块目录按专题组织知识关系。同一文章可以进入
   不同专题，但只维护一份正文和稳定身份。
7. 每篇正文至少核对：问题背景、逐步定义、最小片段、完整 C++17 代码、复杂度、
   边界、常见错误、需要记住的内容和四选项小测。并非每篇机械使用同一组标题。

## 当前结构的主要问题

| 当前结构 | 问题 | 整改 |
| --- | --- | --- |
| 第一个竞赛程序 | 名称偏离原始设计 | 改为“第一个程序” |
| 数值、变量与算术 | 把整数、浮点、名称规则和运算混在一起，同时缺少数学函数 | 拆成“整数类型”和“浮点类型”；变量只给最小解释；补数学函数 |
| 进制与位运算 | 进制表示被拆散，转换程序又位于尚未学习循环、数组、字符和函数的位置 | 恢复独立“进制表示”单元并移动到函数之后；位运算移入第 02 章 |
| 布尔、比较与逻辑和条件语句分离 | 布尔类型的行为与条件结构边界不清 | 布尔类型独立；比较和逻辑跟随“条件语句” |
| 循环与数组相邻 | 数组虽然依赖循环，但还有独立的存储、初始化、越界和多维布局问题 | 保持“循环语句”和“数组”两个独立单元，按依赖相邻排列 |
| 字符、字符串与输入输出 | 把字符类型、文本结构和通用 I/O 接口强行合并 | 拆成“字符类型”“字符串”和“输入输出”三个单元 |
| 函数早于数组和字符串 | 参数文章无法自然讨论数组、C 字符串与 `string` | 函数移动到数组和字符串之后 |
| 算法入门混入具体算法 | 概念地图和完整算法争夺同一章边界，导致第 03 章不断膨胀 | 第 03 章只保留复杂度、算法设计思想和标准库基础；其余算法从第 04 章按前向依赖展开 |
| 质数文章同时承担概念与试除法 | 定义和算法边界不够聚焦，缺少两种典型复杂度的对照 | 收窄质数概念，新建“试除法” |
| 埃氏筛位于高中基础 | 初中数论缺少批量找质数的自然结尾 | 移入初中数论基础；欧拉筛仍留高中基础 |
| 欧拉函数与莫比乌斯函数位于高中基础 | 与最新分级不符 | 移入高中进阶“数学：数论函数” |
| 矩阵单元混入线性变换，却没有独立突出线性递推应用 | 主线目标不清楚 | 高中基础只保留矩阵运算、快速幂和线性递推；线性变换另归专题 |

## 当前正文、身份与小测比对

下面的“现状”以 2026-08-16 工作区中的 `notes/learning-path.md`、
`notes/catalog.md`、正文文件和同名 `.quiz.json` 为准。这里先记录迁移证据，不把
计划中的新文件伪装成已经存在。

### 第 01 章现状矩阵

| 目标单元 | 当前正文与小测 | 已确认整改 |
| --- | --- | --- |
| 第一个程序 | `010101` 与 `010102` 都有正文；`010102` 有四题小测，`010101` 没有小测 | 单元改名；保留两篇稳定 `article_key`；根据 Hello World 的真实检验价值决定补一份极短小测，不能为了配额制造无意义问题 |
| 整数类型 | `010201`、`010202`、`010203` 有正文和小测 | 保持独立；机器表示移动到“进制表示” |
| 浮点类型 | `010301`、`010302`、`010303` 有正文和小测 | 保持独立；IEEE 754 仍为附属扩展 |
| 布尔类型 | `010401` 有正文和小测 | 独立成单元，不为了篇数并入条件语句 |
| 条件语句 | `010501`、`010502`、`010503`、`010504` 均有正文和小测 | 保持比较、逻辑、`if` / `else`、`switch` 聚焦；`switch` 首次解释 `break` |
| 循环语句 | `010601`、`010602`、`010603`、`010604` 均有正文和小测 | 保持独立单元；循环回访 `break` 并引入 `continue` |
| 数组 | `010701`、`010702`、`010703` 均有正文和小测 | 用常量定义数组容量，再解释初始化、越界和行优先连续布局 |
| 字符类型 | `010801` 有正文和小测；字符分类与转换没有独立正文 | 从现有内容提炼字符分类与转换正文 |
| 字符串 | `010901`、`010902`、`010903` 均有正文和小测 | 保留 C 字符串、`string` 和整行输入；移出通用输入输出 |
| 函数 | `011001`、`011002`、`011003`、`011004`、`011005` 均有正文和小测 | 整体移动到数组和字符串以后；补函数原型与互相调用的动机；保持调用栈和递归分篇 |
| 输入输出 | `011101`、`011102`、`011103`、`011104` 均有正文和小测；当前与字符和字符串合并 | 保持输入、输出、格式控制和文件重定向的文章身份，按 C 接口、C++ 流、格式与文件重新审查边界 |

### 第 02 章现状矩阵

| 目标单元 | 当前正文与小测 | 已确认整改 |
| --- | --- | --- |
| 进制表示 | 位、字节与整数机器表示已有正文；进制位权和转换程序缺失 | 保持核心身份；新建进制位权与转换程序；IEEE 754 和平台位宽保留附属扩展 |
| 位运算 | `020201` 同时包含按位和移位运算，并有小测 | 原文件收窄为按位运算；新建移位运算及其小测；从第 01 章移入第 02 章 |
| 指针 | 地址与指针正文已经存在 | 地址在指针正文中引入；相关 `const` 保留扩展 |
| 引用 | 引用与参数传递正文已经存在 | 独立成单元，并用参数传递收束值、指针与引用三种形式 |
| 结构体 | 结构体、联合体和枚举已有正文 | 结构体为核心；联合体和枚举作为并列扩展；内存对齐为附属扩展 |
| 类 | 类相关正文较齐全，但缺独立“封装” | 单元名只写“类”；补封装，拆开函数重载和运算符重载 |
| 内存 | 内存区域、存储期、生命周期、初始化和 `static` 已有草稿 | 使用准确的基础术语；不把这些内容误称为 C++“内存模型” |
| 编译 | 预处理、宏、条件编译、编译和链接正文已有草稿 | 放在本章末尾；不合并聚焦文章 |

### 新文章和 ID 预留

文章已统一使用六位 `CCUUPP` 主入口 ID。新节点不再从模块的旧编号池中取号。2026-08-16 迁移同时更正了部分语义路径，并按用户授权不保留旧已读、作答或评论身份的兼容层；新的 `article_key` 在本次迁移后开始保持稳定。

| ID | 文章 | 文件 | 属性与来源 |
| --- | --- | --- | --- |
| 020101 | 位、字节与存储单位 | `cpp/bits-bytes-and-storage-units.md` | 由整数类型的旧附属页提升为核心 |
| 020103 | 有符号整数的机器表示 | `cpp/integer-binary-representation.md` | 由整数类型的旧附属页提升为核心 |
| 010303 | 数学函数 | `cpp/cmath-functions.md` | 新建核心正文与小测 |
| 010802 | 字符分类与转换 | `cpp/character-classification-and-conversion.md` | 新建核心正文与小测 |
| 020102 | 进制表示 | `cpp/base-notation.md` | 新建核心正文与小测 |
| 020104 | 进制转换 | `cpp/base-conversion.md` | 新建核心综合正文与小测 |
| 020202 | 移位运算 | `cpp/shift-operators.md` | 从 `020201` 拆出的第 02 章核心正文与小测 |
| *020603 | 封装 | `cpp/encapsulation.md` | 第 02 章独立扩展正文与小测 |

旧导航身份已在统一迁移中删除，不与新 ID 重复登记；`*010201e3` 与 `*010301e1` 继续分别配套整数类型和浮点类型。完整新旧对照见 [文章身份迁移清单](article-identity-migration.json)。

#### 现有文章与小测补充

- 指针、结构体、内存、源码处理和类的现有正文大多已经配有小测，可以按新单元
  重排，不需要复制另一套文件。
- `*990101` 变量和 `*990102` 字面量只由模块目录查阅；第 01 章在首次使用处给出最小
  解释。`010701` 常量进入数组单元，用固定容量建立其实际用途。
- 类相关内容目前已有 `*020601`、`*020602`、`*020604`、`*020605`、`*020606`、`*020607`、
  `*020609`、`*020610`、`*020608`，但没有独立“封装”；使用 `*020603` 补齐。
- `operator-overloading.md` 当前没有配套小测；正文实质修订时补齐。IEEE 754、平台
  位宽等纯辅助查阅扩展可以明确“不要求理解和记忆”，不机械补题。

### 第 03 章现状矩阵

| 目标单元 | 当前位置 | 迁移 |
| --- | --- | --- |
| 复杂度 | 第 03 章，正文与小测已存在 | 保持；均摊复杂度和势能法继续后移 |
| 算法设计思想 | 第 03 章，正文与小测已存在 | 保持方法地图；不把具体枚举和贪心题型塞回总览 |
| 标准库基础 | 第 03 章，正文与小测已存在 | 保持；`array` 继续为扩展 |
| 前缀和 | 旧第 03 章，`030401`、`030402` 正文与小测已存在 | 整个单元移入第 04 章开头；差分仍是前缀和的派生方法 |
| 排序 | 旧第 03 章，六种核心排序和比较正文、小测已存在 | 移入第 04 章；快速、归并成为分治和双指针的正式例子 |
| 二分 | 旧第 03 章，查找、边界、标准库接口、答案二分和实数二分正文、小测已存在 | 移入第 04 章，不与离散化合并 |
| 离散化 | 旧第 03 章已有独立单元、正文与小测 | 移入第 04 章并保持独立单元 |
| 双指针 | 旧第 03 章，`030801`、`030802` 正文与小测已存在 | 放在线性表以后；主线回访快速、归并扫描，链表应用仅作后向扩展 |

上述迁移会同步改变六位课程 ID，但正文运行时身份仍由语义路径形成的 `article_key`
表示。第 04 章按前缀和、排序、二分、离散化、线性表、双指针、栈、队列、贪心
及后续主题重新编号，并同步重编文章 ID。

## 第 01 章：C++ 基础

目标顺序固定为：第一个程序 → 整数类型 → 浮点类型 → 布尔类型 → 条件语句 →
循环语句 → 数组 → 字符类型 → 字符串 → 函数 → 输入输出。

### 单元 01：第一个程序

#### `Hello World!`（复用 `cpp/hello-world.md`）

- 保留已经审阅的最小程序，不把它扩成语法总览。
- 展示 `#include <bits/stdc++.h>`、`using namespace std;`、`solve`、`main`、
  `return 0` 和 `cout << "Hello World!" << '\n';`。
- 只说明 `cout` 用于输出、`'\n'` 表示换行；其他部分先作为固定结构。
- 小测只检查能够识别正确输出和换行，不考尚未学习的预处理或命名空间规则。

#### `A+B Problem`（复用 `cpp/a-plus-b-problem.md`）

- 先用 `int a = 3; int b = 5;` 展示可以给整数预先确定值，再改成 `cin >> a >> b`。
- 示例输入必须是 `3 5`，示例输出必须是 `8`，输出末尾换行。
- 解释 `int` 只到“声明一个整数”，完整整数规则留到下一单元。
- 说明 OJ 只比较输入输出，并介绍 AC、WA、TLE、MLE、RE 等最常见结果。
- 洛谷提交作为练习的一部分，不讲账户注册。

### 单元 02：整数类型

#### 整数类型（收窄 `cpp/integer-types.md`）

- 从最常用的 `int` 开始，说明竞赛环境中通常为 32 位、4 字节，并用 `sizeof`
  观察；同时区分标准最低保证和常见环境事实。
- 核心只建立 `int`、`long long`、取值范围、十进制字面量和溢出边界。
- 正文说明使用“32 位整数”“64 位整数”；代码可用 `ll`，说明文字不把类型简称
  当作数学范围。
- 有符号溢出是未定义行为；常见二补码现象只能作为观察，正确代码不能依赖成环。
- `short`、`long`、完整平台位宽和特殊整数类型移到现有附属扩展。
- 位和字节只在首次出现时给最小解释，完整单位、进制与机器表示留到后续独立单元。

#### 算术表达式（收窄 `cpp/arithmetic-expressions.md`）

- 覆盖加、减、乘、除、余数、一元正负号和括号。
- 解释整数除法向零截断，包含正负数例子；除数不能为零。
- 明确 `^` 是按位异或，不是乘方；乘方和开方交给数学函数文章。
- 只介绍当前需要的优先级，复杂表达式优先加括号，不要求背完整表。

#### 复合赋值（复用 `cpp/compound-assignment-operators.md`）

- 解释“读取旧值、计算、再写回”，而不是只列 `+=`、`-=`、`*=`、`/=`、`%=`。
- 说明复合赋值不会消除除零、截断、范围和溢出问题，最后仍转换回左侧类型。
- 自增自减不在这里展开，移动到控制流单元。

### 单元 03：浮点类型

#### 浮点类型（收窄 `cpp/floating-point-types.md`）

- 从“小数为什么叫浮点数”开始，介绍 `float` 与 `double` 的用途和大致十进制
  有效数字，不写成每个数都固定精确到 7 位或 15 位。
- 解释科学计数法字面量与输出、`fixed` 和 `setprecision` 的最小用法。
- 明确浮点数保存近似值；具体 IEEE 754 字段、隐藏位、移码、正负零、无穷和 NaN
  进入附属扩展，不要求理解和记忆。

#### 数值类型转换（收窄 `cpp/type-conversions.md`）

- 覆盖初始化、赋值、混合算术和返回值发生的隐式转换。
- 明确整数除法完成以后再转浮点已经来不及；至少一个操作数先变成浮点数。
- 浮点转整数是向零截断，不是四舍五入；上取整、下取整和四舍五入由数学函数完成。
- 说明窄化和有符号/无符号混合风险；不用生僻强制转换写法污染竞赛示例。

#### 数学函数（新建正文）

- 介绍 `<cmath>` 中当前真正需要的 `sqrt`、`pow`、`floor`、`ceil`、`trunc` 和
  `round`，并给出输入、返回值和典型用途。
- `pow` 主要按浮点语义工作，返回结果可能有舍入误差，不能代替保证精确的整数幂。
- 整数指数先用循环，快速幂在算法/数论阶段学习。
- 向上取整除法公式必须注明正整数前提和加法溢出风险，不宣称
  `(a + b - 1) / b` 对任意整数成立。

#### 本单元移出的文章

- `variable-declaration-initialization.md`：只保留为模块查阅文章；早期正文在
  第一次使用变量时给出最小解释。
- `const.md`：移动到数组单元，用固定数组容量建立真实用途。
- `literals.md`：不再进入主学习路线；内容拆回整数、浮点、字符、字符串和进制
  对应文章，原文件保留为模块查阅文章。
- `constexpr.md`：继续作为扩展，不阻塞核心路线。
- `cpp/integer-type-widths.md`：平台位宽、`short` / `long` 和数据模型继续作为整数
  类型的附属扩展。

### 单元 04：布尔类型

#### 布尔类型（复用 `cpp/boolean-type.md`）

- `bool` 只有 `false` 和 `true`；整数转布尔时零为假、非零为真。
- `sizeof(bool)` 通常是一字节不表示有 256 个逻辑值；`bool` 没有 `unsigned` 版本。
- `!!x` 只表示连续两次逻辑取反，把条件规范成布尔值，不包装成背诵技巧。

### 单元 05：条件语句

本单元按学习任务自顶向下组织：先提出“程序需要根据当前情况选择不同语句”，再用
比较运算和逻辑运算构造条件，最后落实到 `if` / `else` 与 `switch`。

#### 比较运算（复用 `cpp/comparison-operators.md`）

- 六种比较、严格/非严格边界、赋值与相等的区别。
- 必须用独立小节解释 `1 <= x <= n` 为什么错误：第一次比较先得到 `bool`；正确
  写法是 `1 <= x && x <= n`。
- 浮点比较的误差边界保持为配套内容，不让它掩盖整数比较主线。

#### 逻辑运算与短路求值（复用 `cpp/logical-operators.md`）

- `!`、`&&`、`||` 从题意逐步组成条件。
- 短路用于先检查边界或空值，也意味着右侧副作用可能不执行。
- 区分逻辑与、或和按位与、或；混合条件用括号提高可读性。

#### `if` 与 `else`（复用现有正文）

- 直接使用前面得到的布尔表达式选择互斥分支。
- 解释单分支、二分支、连续条件、嵌套和悬空 `else`，不把条件语句与分支语句当成
  两类不同结构。

#### `switch`（复用现有正文）

- 解释选择表达式、`case`、`default` 和贯穿行为，并在这里第一次使用 `break`
  阻止继续贯穿。
- 与 `if` / `else` 比较适用场景，不把 `switch` 说成能替代任意复杂布尔条件。

### 单元 06：循环语句

本单元只回答怎样重复执行。数组依赖循环完成输入、遍历和修改，但具有独立的存储、
初始化、越界和多维布局问题，因此进入下一单元。

#### `while`（复用现有正文）

- 从“条件仍然成立就继续执行”推导循环结构，先处理次数暂时未知的过程。
- 回访已经在 `switch` 中见过的 `break`，再与 `continue` 对比；不再为二者设置
  独立文章。

#### `for`（复用现有正文）

- 依次解释初始化、条件、更新，并用确定次数的计数过程建立主用法。
- 学完以后马上用于遍历数组；嵌套循环随后服务于多维数组。

#### 自增与自减（复用 `cpp/increment-decrement-operators.md`）

- 放在循环以后解释，普通计数优先把 `i++` 单独成句。
- 前缀/后缀差异只在结果被使用时出现；避免在复杂表达式中依赖求值细节。

#### `do while`（低频独立扩展）

- 保留“至少执行一次”的唯一核心区别和读入示例。
- 标记为低频或扩展，放在其他循环之后，不把它与 `while` 合篇。

### 单元 07：数组

#### 常量（复用 `cpp/const.md`）

- 从 `const int MAXN = 1005;` 引入不可修改的常量对象，并把它用于静态数组容量。
- 解释标准 C++ 数组长度需要常量表达式；“对象不可修改”和“表达式能在编译期
  求值”是两件相关但不相同的事。
- 当前只建立竞赛中固定容量所需的规则；`constexpr` 保留为附属扩展。

#### 一维数组（复用 `cpp/one-dimensional-arrays.md`）

- 元素类型、容量、实际长度、下标、遍历和连续存放。
- C++ 原生数组从 0 开始；本书自定义题目对象通常从 1 开始，两套规则不得在同一
  算法中来回切换。
- 完整比较无初始化器、`{}`、部分初始化列表；越界同时包括负下标和超过右端。
- 局部内置数组不保证自动清零只先作为安全规则，原因留到内存与存储期。

#### 多维数组（复用 `cpp/multidimensional-arrays.md`）

- 行列下标、嵌套遍历、各维容量相乘和行优先连续布局。
- 只使用下标和相邻地址观察布局，不要求先学指针。
- 主文明确“数组不能作为函数参数传递”是错误说法；形参调整和后续维度规则留给
  指针后的附属扩展。

#### 附属扩展

- `cpp/multidimensional-array-layout-and-parameters.md`：数组形参调整、后续维度、
  高维指针和数组引用；在第 02 章学过指针以后阅读。
- `cpp/array.md`：标准库 `array`，继续作为标准库基础的可跳过扩展。

### 单元 08：字符类型

#### 字符类型（复用 `cpp/character-type.md`）

- 字符也是整数类型，但具有文本输入输出、编码和转义等独立行为。
- 解释 ASCII 常用范围、数字字符和值的转换、单引号与双引号。
- 明确普通 `char` 的有符号性由实现决定，并区分 `char`、`signed char`、
  `unsigned char`。
- 转义字符至少包含空字符 `\0`、换行 `\n` 和制表符 `\t`。

#### 字符分类与转换（新建正文）

- 介绍空白、数字、字母、字母数字判断，以及大小写转换。
- 对应 `isspace`、`isdigit`、`isalpha`、`isalnum`、`tolower`、`toupper`；说明
  返回值按真假使用，不依赖其恰好等于 1。
- 数字字符转数值、数值转数字字符，与进制转换互相引用。

### 单元 09：字符串

#### C 字符串（复用 `cpp/c-strings.md`）

- 字符数组、末尾 `\0`、容量与长度、`strlen` 和常用 `<cstring>` 接口。
- `%s` 跳过开头空白并在下一个空白处停止；必须为末尾空字符留空间。
- 不把字符 `'0'` 与空字符 `'\0'` 混淆。

#### `string`（复用 `cpp/string.md`）

- 明确它是 C++ 标准库类型，保留原生 0-based 和左闭右开接口。
- 覆盖空字符串、长度、下标、遍历、拼接、比较、`substr` 和 `find` 的最小常用集。
- 不把 UTF-8 字节数量误当成所有文字的字符数量。

#### 整行输入（复用 `cpp/whole-line-input.md`）

- 比较 `getline`、`cin.getline`、`fgets`，并解释数值提取遗留换行。
- 同时回顾：`%c` 默认不跳过空白，`%s` 的停止规则，`cin >> ch` 默认跳过空白，
  `cin.get` 不跳过。
- 本文属于文本处理单元；通用接口和格式状态在输入输出单元系统整理。

### 单元 10：函数

函数放在数组和字符串之后，使参数文章可以直接使用这些已知类型。

#### 函数的定义与调用（复用现有正文）

- 定义、调用、`solve`、`main`、先声明后使用。
- 加入函数原型，并用两个函数互相调用说明为什么只靠定义顺序不够。

#### 返回值（复用现有正文）

- 返回类型、`return` 结束当前调用、所有路径返回、`void` 和 `main` 的 `return 0`。
- 返回值不是输出；返回时发生的类型转换与数值转换文章互相引用。

#### 形参与实参（复用并扩充现有正文）

- 数量、顺序、类型和值参数副本。
- 现在可以用数组、C 字符串和 `string` 对比接口，但只陈述当前能准确解释的最小
  规则；指针/引用形式和完整参数传递移动到第 02 章。
- 不传播“数组不能传参”的错误说法。

#### 调用栈与递归（保留两篇相邻正文）

- 调用栈解释每次调用独立状态、嵌套调用、逐层返回、局部对象边界和栈溢出。
- 递归解释终止条件、参数靠近终点、向下进入子问题和向上恢复答案。
- 第 03 章的递归与问题分解只讨论算法思想，不重复 C++ 函数语法。

### 单元 11：输入输出

#### `scanf` 与 `printf`（新正文 `cpp/scanf-and-printf.md`）

- 把 `scanf` 与 `printf` 放在同一接口体系中学习，覆盖常用格式说明符、空白处理、
  固定符号、百分号、返回值和类型匹配。
- 特别辨析 `%c`、`%s`、整数和浮点格式；`double` 输入与输出格式不同。

#### `cin` 与 `cout`（新正文 `cpp/cin-and-cout.md`）

- 把 `cin` 与 `cout` 放在同一接口体系中学习，覆盖提取、插入、失败状态、换行、
  流同步和不能随意混用两套接口。
- 说明 `endl` 还会刷新缓冲区，普通换行优先使用 `'\n'`。

#### 输出格式控制（重写 `cpp/output-formatting.md`）

- 对照两套接口整理定点、科学计数、有效数字、小数位数、宽度、补零、补空格、
  左右对齐和显式正号。
- 明确格式状态是否持续、宽度只是最小宽度、格式化不会修改变量。
- 回访进制输出和浮点格式，但不重复类型原理。

#### 文件输入输出与重定向（复用 `cpp/file-redirection.md`）

- `freopen` 的位置、读写模式、文件名和工作目录。
- 普通 OJ 不要求文件名时不要保留题目专用重定向。

## 第 02 章：C++ 进阶

本章的前五个单元进入连续核心路线；类、编译、名称和内存明确标为扩展阅读，不学
这些扩展不阻塞第 03、04 章。系统整理不能反过来成为早期程序的隐藏前置。

### 单元 01：进制表示

#### 位、字节与存储单位（复用 `cpp/bits-bytes-and-storage-units.md`）

- `bit`（比特/位）是最小的信息单位；字节是 C++ 中 `sizeof` 和地址递增使用的
  基本存储单位，二者不得混称。
- 解释 1 字节通常为 8 位，以及 B、KB、MB、GB、TB 的中文名和常见换算口径。
- 表格保持简洁；“计算机资料也常写 KB”等说明放在表格下方。
- 解释 Mbps 中 `b` 与文件大小中 `B` 的区别，以及中文“兆”的歧义。

#### 进制位权与字面量（新建 `cpp/base-notation.md`）

- 从位权解释二进制、八进制、十进制和十六进制，说明八进制和十六进制是二进制
  的紧凑写法。
- 介绍 C++ 的 `0b`、前导 `0` 和 `0x` 字面量；十六进制示例统一使用小写字母。
- 先学习阅读位串和纸笔完成小规模转换，再进入程序实现。

#### 有符号整数的机器表示（复用 `cpp/integer-binary-representation.md`）

- 分别解释原码、反码、补码，不把二补码错误说成“符号位加 31 个普通数值位”。
- 推导 32 位二补码整数的最小值与最大值为什么不对称。
- 说明初赛可能考查，要求理解原理；不要求依赖有符号溢出行为写程序。

#### 浮点数的机器表示（配套现有 `cpp/ieee-754.md`）

- 先用二进制小数位权解释有限与无限展开，再区分定点表示和浮点表示。
- IEEE 754 的符号、指数、有效数字、隐藏位、正负零、无穷和 NaN 作为附属扩展，
  只用于解释行为，不要求理解和记忆完整编码。
- 明确 `std::fixed` 只是输出格式，不会把 `double` 变成定点数类型。

#### 进制转换（新建 `cpp/base-conversion.md`）

- 从位权展开和除基取余分别推导两个方向的纸笔转换，再翻译成程序。
- 定义 `'0'` 到 `'9'`、`'a'` 到 `'f'` 与数值之间的双向映射；先判断字符是否
  合法，再转换。
- 从位权展开推导逐位累积 `value = value * base + digit`，实现任意进制转十进制。
- 从反复除基取余推导逆序数字，实现十进制转任意进制，并显式处理输入为 0。
- 把字符转数值、数值转字符和两个方向的转换分别写成函数，形成循环、字符串、
  字符转换和函数的综合练习。基础版本只处理非负整数。

### 单元 02：位运算

#### 按位运算（收窄 `cpp/bitwise-operators.md`）

- 分别推导 `&`、`|`、`^`、`~`，并与逻辑运算明确区分。
- 用位掩码解释读取、设置、清除和翻转第 `k` 位。
- 再次强调 `^` 不是乘方；混合运算用括号表达意图。

#### 移位运算（从现有文章拆出新正文）

- 单独解释 `<<`、`>>`，以及移位与乘除 2 的关系何时成立。
- 说明运算前的整数提升、类型宽度、`1LL << k`、移位位数范围和溢出风险。
- 对 C++17，负有符号整数右移结果由实现决定；无符号运算按 $2^N$ 取模。
- 不把 `bitset` 塞入本篇；它属于后续标准库工具。

### 单元 03：指针

- 指针正文先从地址引入，再解释声明、取地址、解引用、空指针、指针算术、数组到
  指针的转换，以及 `a[i]` 等价于 `*(a + i)`。`i[a]` 只作语义辨析，不推荐使用。
- 字节寻址和指针/引用中的 `const` 保留为模块查阅或附属扩展，不为了收录现有文件
  打断教学任务。
- 数组不是指针；数组表达式通常转换为首元素指针，函数形参中的 `T a[]` 会调整为
  `T* a`。多维数组参数、数组指针、高维指针和数组引用进入数组附属扩展。

### 单元 04：引用

- 引用正文从 C 只能用指针间接修改调用者对象的麻烦引入 C++ 引用，比较声明、绑定、
  使用和不可重新绑定等差异；同时辨析 `&` 在声明与表达式中的不同含义。
- 参数传递回访形参（parameter）与实参（argument），比较值传递、指针传递和引用
  传递，并说明修改 `vector` 时通常传引用。

### 单元 05：结构体

- 使用标准中文“结构体”，不称“记录类型”。
- 结构体、成员和点运算；重点解释聚合相关数据、参与排序和使用 `sizeof`。
- 学过指针以后再讲结构体指针与箭头；内存布局和对齐作为附属扩展。
- 联合体与枚举不是结构体的下级类型，也不进入本单元；只保留为模块查阅扩展。

### 单元 06：*类

- 单元名只写“类”；正文仍准确解释类、对象、成员变量和成员函数。
- 新建独立的“封装”正文；访问权限覆盖 `public`、`private`、`protected`，友元
  作为访问权限的下级内容。
- 构造函数、析构函数、函数重载、运算符重载分别成篇。
- 继承包含多继承和不同访问方式；多态解释虚函数、通过基类指针/引用调用派生行为、
  虚析构和对象切片。
- 虚表与虚表指针只作为主流实现模型，不写成标准规定的固定对象布局。

### 单元 07：*编译

- 预处理、`#include`、`#define`、条件编译、编译、链接各自聚焦。
- 宏定义说明替换范围直到 `#undef` 或翻译单元结束；登记 `#undef`。
- `#if`、`#ifdef`、`#ifndef`、`#elif`、`#else`、`#endif` 属于条件编译。
- `inline` 只留扩展，不暗示手写内联能替代编译器优化。
- 命名空间作用域的 `static` 控制内部链接，在链接文章解释。

### 单元 08：*名称

- 分篇整理关键字、标识符、作用域、名称遮蔽、命名空间、`typedef` 和 `using`。
- 整数类型正文第一次出现 `typedef long long ll;` 时给出最小解释；
  `using namespace std;` 在最早程序中先作为固定结构。
- 本单元用于系统查阅，不成为算法主线前置。

### 单元 09：*内存

本单元讲竞赛程序的内存布局、存储期和对象生命周期。“内存模型”在 C++ 中已有
涉及并发、原子操作和内存顺序的特定含义，不用它指代这里的基础内容。

- 常见四个内存区域、高地址/低地址只作为竞赛环境示意，不写成 C++ 标准唯一布局。
- 无初始化器的局部内置整数和数组元素不会自动得到零；静态存储期对象先零初始化；
  `int x{}` 和 `int a[10]{}` 仍会零初始化。
- `string s;`、`vector<int> a;` 执行默认构造得到空对象；`vector<int>(n)` 创建并
  值初始化元素，`reserve(n)` 只改容量、不改 `size()`。
- `static` 局部变量的名称仍是块作用域，对象具有静态存储期；不能称“局部作用域的
  全局变量”。类中的 `static` 成员留在类成员文章，不在这里合并解释。
- 指针在常见 32/64 位环境通常为 4/8 字节，但指针宽度、地址空间、整数位宽和
  机器字长不是同一概念。
- `volatile` 只保留模块目录扩展；不能当线程同步工具。

#### 初始化（新增 `cpp/initialization.md`）

- 集中比较局部内置对象、静态存储期对象、花括号初始化和类类型默认构造；不把
  “没有显式初值”概括成一条适用于所有类型与存储期的规则。
- 对比空 `string`、空 `vector`、`vector<int>(n)`、`resize`、`assign` 与
  `reserve`；特别说明 `reserve` 不创建元素，也不改变 `size()`。


## 第 03 章：算法入门

本章只保留三个单元。现有前缀和、排序、二分、离散化和双指针正文不会删除，但其
权威学习入口整体迁移到第 04 章；正文路径继续作为稳定 `article_key`。

### 单元 01：复杂度

#### 复杂度基础（复用 `algorithm-basics/complexity.md`）

- 时间/空间复杂度、输入规模、最坏情况、大 $O$ 和常见数量级。
- 小 $o$、$\Omega$、$\Theta$ 放入 `asymptotic-notation.md` 扩展；当前只要求会用
  大 $O$ 描述上界。

#### 从代码分析复杂度（复用 `algorithm-basics/complexity-analysis.md`）

- `i++` 的线性循环、`i *= 2`、`n /= 2` 的对数循环；`n >>= 1` 等到可跳过的
  位运算单元学习以后再回访。
- 顺序代码相加、嵌套循环相乘、依赖外层边界的三角形循环。
- 两个下标互相影响或单调移动时按总移动次数分析，不能机械看成两层循环平方。
- 均摊复杂度和势能法留到高中阶段第二个复杂度单元。

### 单元 02：算法设计思想

- 枚举：只讲循环遍历候选；子集、排列、组合和 DFS 枚举以后展开。
- 模拟：按题目规则维护状态，不把复杂题型堆进总览。
- 递归与递推：用较小问题定义当前问题；斐波那契等只建立直觉，不提前当作动态
  规划。
- 分治：这里只建立拆分、递归求解、合并的地图；快速/归并排序以后完整回访。
- 贪心：只建立“局部选择必须证明安全”；邻项交换和反悔策略进入第 04 章。
- 动态规划作为方法地图中的名称，说明会保存重复子问题；阶段、状态、决策在正式
  动态规划单元学习。

### 单元 03：标准库基础

- `vector`：大小/容量、0-based、遍历、`push_back`，并集中辨析 `assign`、
  `resize`、`reserve`、`reverse`；预留容量不改变 `size()`。
- 迭代器先给最小概念；说明扩容可能使 `vector` 的迭代器/引用/指针失效，遍历中
  插入或删除需要重新审查。
- `fill`：覆盖已有范围、不改变容器大小；`memset` 只作为按字节写入的陷阱辨析。
- `pair`、`tuple` 分篇；`array` 为同层扩展。
- `sort`：默认顺序、逆序、多关键字排序、比较函数、结构体比较和严格弱序；优先
  利用 `pair` / `tuple` / `array` 的字典序减少自定义比较。
- `unique`：只把相邻重复元素移到逻辑末尾，通常先排序，再配合 `erase`；不称泛化
  的“去重函数”。
- `min`、`max`、`swap`、`reverse` 在第一次实际使用时解释，不为凑目录各开总览。

## 第 04 章前半部分的迁移单元

以下内容从旧第 03 章迁出，并置于原有线性表、栈、队列、贪心、数论、图论等单元
之前或之间。迁移时统一重编六位学习路线 ID、模块目录 ID 和迁移清单中的当前 ID，
不改语义文件名和题目 ID。

### 单元 01：算法基础：前缀和

#### 前缀和（移动 `algorithm-basics/prefix-sums.md`）

- 从重复计算静态区间和的暴力解法引入累计数组。
- 推导 `prefix[i] = prefix[i - 1] + a[i]` 和闭区间查询
  `prefix[r] - prefix[l - 1]`，解释 `prefix[0] = 0` 的边界语义。
- 比较 $O(n)$ 预处理、$O(1)$ 查询与直接枚举的复杂度；明确数组发生修改后原前缀和
  不再有效。

#### 差分（移动并收窄现有差分正文）

- 从“怎样一次记录区间加法，而不逐个修改元素”引入差分。
- 推导 `difference[l] += value`、`difference[r + 1] -= value`，再用一次前缀和
  恢复最终数组。
- 明确差分是前缀和的逆过程，并区分离线批量修改与在线修改/查询。

#### 附属扩展

- 二维前缀和与二维差分继续作为本单元附属内容，不阻塞主学习路线。
- 普通前缀和或差分无法处理在线交错修改和查询时，后向引用树状数组与线段树。

### 单元 02：算法基础：排序

#### 三种平方复杂度排序（按聚焦程度保留或拆分现有正文）

- 冒泡排序、选择排序、插入排序分别解释每轮确定了什么，不只给三段模板。
- 三者都分析 $O(n^2)$，同时比较交换次数、最好情况和稳定性。

#### 计数排序

- 从整数值域较小的条件引入计数数组，说明时间和空间都依赖值域。
- 与广义桶排序保持术语边界：本文学习的是每个整数值直接计数的计数排序。

#### 快速排序（移动现有正文）

- 从选择基准、划分区间、递归处理两侧逐步推导。
- 展示相向双指针怎样找到需要交换或停止的位置，给后面的双指针单元留下正式回访。
- 说明平均、最坏复杂度、递归深度和通常不稳定。

#### 归并排序（移动现有正文）

- 从合并两个有序区间推导完整分治过程。
- 展示两个同向指针如何各自只前进，并为后面的双指针单元提供第二个核心例子。
- 说明稳定性、$O(n \log n)$ 时间与 $O(n)$ 辅助空间。

#### 排序性质比较

- 统一比较最好、平均、最坏时间复杂度、额外空间和稳定性。
- 选择排序、快速排序和堆排序通常不稳定；其余结论以正文所教的具体实现为准。
- 堆排序、基数排序为扩展正文；希尔排序只登记名称和定位。

### 单元 03：算法基础：二分

#### 二分查找（移动现有正文）

- 在有序数组上从排除一半候选推导手写闭区间模板。
- 对照 `binary_search`、`lower_bound`、`upper_bound` 的左闭右开迭代器接口。
- 明确循环不变量、中点取整、更新方向、终止条件和不存在答案时的返回语义。
- 说明右移和整数除法在负数时的舍入差异，并避免中点加法溢出。

#### 答案二分（移动现有正文）

- 从答案空间和单调判定函数引入，说明它与“在数组中找一个值”不同。
- 分别整理求最小可行值与最大可行值的闭区间模板。
- 整数与实数二分分开处理；实数版本说明迭代次数或误差终止方式。

### 单元 04：算法基础：离散化

#### 离散化（移动 `algorithm-basics/coordinate-compression.md`）

- 从数值很大但不同值很少、只关心相对次序的问题引入。
- 按排序、`unique`、二分定位三步形成完整算法。
- 区分原数组位置、去重后下标和值的排名；需要恢复原值时保存反向映射。
- 明确离散化保留哪些关系、丢失哪些数值距离，不能无条件替代原坐标。

### 单元 06：算法基础：双指针

#### 双指针（重写 `algorithm-basics/two-pointers.md` 的主线）

- 统一定义为两个下标按可证明的单调方向移动，并在移动时永久排除一批状态。
- 第一组回访快速排序的相向扫描；第二组回访归并排序合并阶段的同向扫描。
- 再用有序数组两数之和建立相向双指针的独立解题模型。
- 所有例子都用总移动次数分析复杂度，不能机械按嵌套层数判断。

#### 滑动窗口（与双指针同单元独立成篇）

- 固定长度窗口和条件决定长度的可变窗口分别推导。
- 只在左右端点进入、离开时增量维护窗口状态，并说明成立所需的单调性。

#### 链表双指针扩展（后向引用，不作为本章前置）

- 合并有序链表，以及环形链表中的快慢指针检测环，必须等到读者学习链表以后。
- 它们作为扩展阅读从本单元提供后向链接；不能为了让双指针文章自洽而把链表提前
  塞进算法入门。

## 初中数论基础的整改

### 因数、倍数与整除（扩充 `math/divisibility.md`）

- 首次写“因数（也称约数）”，后文统一使用“因数”；定义倍数、整除和余数判定。
- 包含 0、正负号和 1 的边界，不把“0 能不能做除数”与“0 是不是倍数”混为一谈。

### 质数与合数（收窄 `math/prime-numbers.md`）

- 只保留定义、1 既不是质数也不是合数、最小质数和简单性质。
- 试除代码与复杂度全部移到独立文章。

### 试除法：质数检测（新建正文）

- 第一种典型写法：从 2 检查到 `n - 1`，时间复杂度 $O(n)$。
- 第二种典型写法：只检查到 $\sqrt n$，用因数成对出现推导 $O(\sqrt n)$。
- 比较 `i * i <= n`、`i <= n / i`、先处理 2 后只枚举奇数等常见变体和溢出边界。
- 对 $n < 2$、2、完全平方数分别测试。
- 必须回答：判断 100 以内的质数或合数时，任何合数都有不超过
  $\sqrt{100}=10$ 的质因数，而不超过 10 的质数只有 2、3、5、7，所以只需检查
  这四个候选；不是说所有因数只有这四个。

### 算术基本定理（复用现有正文）

- 每个大于 1 的正整数都能唯一分解为质数幂乘积；存在性与唯一性分开解释。
- 用指数坐标为最大公约数、最小公倍数、因数个数与因数和建立前置。

### 质因数分解（扩充现有正文）

- 对比从 2 一直尝试、只到平方根、先处理 2 再枚举奇数三类写法。
- 每找到质因数就反复相除并记录指数；循环后剩余值大于 1 时它本身是质因数。
- 解释为什么记录到的候选一定是质数，以及 1、0、负数的边界。

### 埃拉托斯特尼筛法（从高中基础移入本单元）

- 从“逐个试除会重复工作”引入批量标记合数。
- 用 1 到 100 的表格或 SVG 找出全部质数；解释为什么标记一个质数的倍数可以从
  $p^2$ 开始、外层只需到平方根。
- 在例子中再次解释 2、3、5、7 足以发现 100 以内所有合数的理由。
- 本阶段只筛出质数列表或质数个数，不提前把筛法扩展成任意数论函数预处理。

### 最大公约数、最小公倍数与欧几里得算法（复用现有两篇）

- 用质因数指数的最小值/最大值解释 gcd/lcm，本质对应 `min` / `max`。
- 推导乘积关系、0 的约定和避免先乘溢出。
- 欧几里得算法从公约数集合不变推导递归与循环写法。

### 快速幂的阶段决定

- 普通快速幂只依赖整数乘法、指数的二进制分解和循环，可以放在初中数论基础末尾，
  或作为算法技巧紧邻本单元；它不依赖费马小定理。
- 费马小定理依赖同余、质数模和模意义下乘法，保留在高中基础。不能为了名称相近
  把两篇同时提前。

## 高中基础的数论与矩阵整改

### 数学：模运算与同余

- 模运算、负余数规范化、加减乘、逐步取模和模除法不存在直接整数除法。
- 同余作为正式关系独立解释；快速幂在这里回访为模快速幂。
- 费马小定理放在同余和模快速幂以后，明确质数模与互质条件。

### 数学：扩展欧几里得家族

- 扩展欧几里得算法、二元线性不定方程、线性同余方程、非质数模逆元连续排列。
- 每篇先从能解决的典型问题出发，再推导返回的多个量；实现优先使用 `pair` /
  `tuple` 等清晰返回值，不使用无必要的输出引用参数。
- 模逆元同时比较扩展欧几里得方法与质数模下的费马方法，并检查存在条件。
- 中国剩余定理及扩展中国剩余定理放在该家族以后形成独立单元。

### 数学：欧拉筛与因数函数

- 埃氏筛已经在初中完成；这里学习欧拉筛如何让每个合数由最小质因数唯一生成。
- 质数列表、最小质因数表及停止条件必须区分。
- 正因数个数与正因数和跟随本单元；先由质因数指数公式推导，再讨论怎样借助筛法
  批量预处理，不在初中埃氏筛文章中扩张。

### 数学：矩阵与线性递推

- 矩阵表示；矩阵加法与减法；矩阵乘法；矩阵快速幂；矩阵快速幂解线性递推。
- `matrix-exponentiation.md` 收窄为矩阵幂算法；把 Fibonacci 和一般齐次线性递推
  的状态设计、转移矩阵和指数偏移拆成独立应用文章。
- `linear-transformations-as-matrices.md` 不放在这一核心单元；它归入线性代数或计算
  几何相关模块专题。

## 高中进阶与仅目录专题

### 数学：数论函数

- 欧拉函数、欧拉定理、莫比乌斯函数进入高中进阶的推荐专题，不使用“数论进阶 2”
  这类依赖阶段编号的名称。
- 欧拉函数与莫比乌斯函数分别聚焦定义、质因数公式、积性和基础筛法；莫比乌斯反演
  仍是后续独立主题。

### 只进入模块目录

- Berlekamp–Massey、杜教筛、Min_25 筛只登记 `notes/catalog.md` 的扩展专题，
  不进入主学习路线，也不进入学习路线文末扩展索引。
- 这些文章保留规划身份，但不能成为后续核心文章的前置。

## 执行批次

1. 固化最新版原文和本整改计划，清理旧规划中的冲突声明。
2. 只修改第 01 章目录结构和文章登记，确认稳定身份与拆篇去向。
3. 按第 01 章顺序逐篇拆分、补篇、小测和提交；每 15–20 篇运行一次全量目录检查，
   其他时候使用哈希增量内容构建和单篇代码编译。
4. 整理第 02 章，再把第 03 章收束为复杂度、算法设计思想和标准库基础；前缀和、
   排序、二分、离散化和双指针迁入第 04 章，链表双指针保持为后向扩展。
5. 按本计划移动初中数论、数论函数与矩阵专题；移除仅目录算法的学习路线入口。
6. 最后执行目录校验、内容身份测试、增量/全量内容构建和生产构建；除非用户另行
   要求，不部署、不推送。
