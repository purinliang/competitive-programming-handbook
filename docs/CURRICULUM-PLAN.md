# 学习路线重组计划

本文记录已经在逐篇修订中确认、但尚未全部落实到 `notes/learning-path.md` 和正文的教学结构。它用于防止长期协作依赖聊天上下文，不替代两个正式目录：

- `notes/learning-path.md` 是网站学习顺序的唯一来源；
- `notes/catalog.md` 是模块知识注册表；
- 本文只保存重组决策、文章边界和推进状态。

当前章节边界、编号、逐单元内容与执行顺序以
[课程结构最新决策](CURRICULUM-LATEST.md) 为准；逐篇迁移方式见
[课程结构整改执行计划](CURRICULUM-REMEDIATION.md)。
[第 01–05 章重组目标](CHAPTERS-01-05-REORGANIZATION.md) 只是派生摘要；本文后方
未同步删除的旧提案只保留讨论背景，不能覆盖最新版。

正文仍按“一篇一个足够小且明确的主题”逐篇完成。每完成一篇，再同步正式目录、状态、链接和小测验；不能因为本文已经规划就把尚未完成的条目标成已有正文。

## 当前开发范围

自 2026-08-16 起，C++ 基础与 C++ 进阶正式拆成第 01、02 章，原有章节整体顺延。
第 01–03 章经过多轮讨论后已经形成当前稳定骨架，不再进行没有具体教学问题支撑的
整体重排。当前结构探索集中在第 04、05 章：初中基础和初中进阶；发现问题时只做
能够单独阅读和验证的小步调整，不假定一次整理就能得到最终目录。

原“高中基础”和“高中进阶”随编号变成第 06、07 章，仍停止主动开发。已有目录、
正文、题库和模板全部保留；运行全库检查可以读取它们，但不能借检查继续补文章、
补小测或重写正文。

## 推进方式

1. 选择当前学习单元中的一篇文章。
2. 核对标准术语、竞赛圈常用说法和真实使用场景，避免用跨语言上位概念替代 C++ 中的自然名称。
3. 修订或新建正文，并检查它没有提前依赖后面的知识。
4. 同步学习路线、模块目录、引用、稳定文章身份和审阅状态。
5. 运行内容检查；一次提交只固化一个主题或一个不可分割的小组。
6. 用户阅读并提出修改后继续修订；只有用户完整审阅当前版本才标记“已审阅”。

已有文章的语义文件名是协作系统中的稳定 `article_key`。拆分时优先把原文件收窄为最接近的核心主题，新主题使用新文件；确需改名时必须同时设计旧地址和用户数据迁移，不能只修改路径。

## 历史决策记录

> 下列记录保留讨论中形成的文章边界和事实说明。凡是章节编号、单元数量或执行状态
> 与 [第 01–05 章重组目标](CHAPTERS-01-05-REORGANIZATION.md) 冲突的地方，均以
> 新目标和正式目录为准。

以下决策优先于本文后方尚未同步修订的旧分组。先把它们记录为实施依据，再逐项修改正式学习路线、模块目录、正文和解析工具；不能为了让文档表面一致，把尚未完成的迁移写成已经完成。

1. 扩展标记移到 ID 前方：独立扩展写作 `*0233`，附属扩展写作 `*0102e1`。这不是只修改 Markdown 外观：迁移前必须检查并同步修改网站内容构建脚本、目录校验器和其他把 `0233*` 当作固定语法的代码，确保解析得到的稳定文章 ID 仍然不含星号。
2. 函数重载与运算符重载拆成两篇文章。类与对象需要单独讲“封装”；访问权限与友元围绕封装组织，可以拆成两篇，也可以让友元作为访问权限的附属内容，但不能继续把函数重载和运算符重载合成一个主题。
3. 第 02 章只保留算法设计思想的第一次介绍：枚举、模拟、递归与分治、贪心。子集、排列、组合等具体枚举方法移到后续搜索单元；具体贪心构造和复杂模拟也在形成足够内容后分别组织应用单元，不用基础概念文章提前承载所有题型。
4. 深度优先搜索与广度优先搜索分别组成单元。深度优先搜索单元增加“状态空间与隐式图”作为桥梁：循环枚举直接遍历显式编号的候选；DFS 枚举把部分方案看作状态、把一次选择看作状态转移；图的 DFS 则遍历题目显式给出的点和边。这样明确三者的关系，而不让读者把 DFS 枚举与图的 DFS 误认为只是两个偶然同名的模板。
5. 动态规划拆为“动态规划：动态规划基础”和“动态规划：背包”。基础单元依次建立阶段、状态、决策与转移，并通过数字三角形、最大子段和、最长上升子序列和最长公共子序列形成最小模型集合；0-1 背包、完全背包与多重背包另成单元，集中比较物品使用次数和容量循环方向。
6. 字符串的基础单元统一命名为“字符串：字符串基础”。从第 03 章开始，冒号左侧是模块，右侧是拿掉模块前缀后仍然完整的单元名；当模块名与主题名相同，允许这种有规则的重复，不把右侧缩成孤立的“基础”。

本轮先落实第 01、02 章的名称、拆篇与顺序，再按照同一规则重组第 03、04 章；正文修订暴露出新的真实边界时允许继续局部调整，但不得退回按现有文件数量凑单元的做法。

## 第 01–03 章阶段边界的下一轮审查

2026-08-16 的后续讨论发现：当前第 02 章同时承担复杂度、算法设计思想、标准库
工具和若干完整算法，第 03 章又叫“初中基础”，两章对“基础”的职责容易重叠。
下一轮不能只在现有表格内部调换文章，而要先审查三个阶段分别完成什么学习任务。

以下是待落实的结构提案，不表示正式学习路线已经迁移。

### C++ 基础与 C++ 进阶

语义上把 C++ 内容分成两层：

- “C++ 基础”只保留后续主线确实需要的语言与输入输出能力；
- “C++ 进阶”收纳理解语言底层、阅读少见代码或面向对象程序设计时才需要的内容。

“C++ 进阶”不应成为算法主线的强制前置。更合适的出版形式是第 01 阶段中的选读
分支，或扩展阅读索引中的完整单元；网站的核心上一篇、下一篇自动跳过带星文章。
这样既使用中文资料中通行的名称，也不会暗示读者必须学完继承、多态才能进入算法。

C++ 基础至少保留：

1. Hello World! 与 A+B Problem；
2. 数值、布尔、字符等实际需要的类型；
3. 算术、比较与逻辑；
4. 条件、循环和函数；
5. 一维数组与多维数组；
6. 字符串；
7. 基础输入输出；
8. 后续代码真正需要的最小引用和结构体用法。

C++ 进阶候选内容包括：

- 地址、原生指针、指针运算和复杂的指针 `const` 组合；
- 多维数组参数调整、高维指针和更完整的参数传递规则；
- 内存区域、存储期、对象生命周期、动态分配和 `static` 的不同语境；
- 指针宽度、数据模型与机器字长；
- 结构体内存布局、联合体与枚举；
- 类与对象、封装、访问权限、友元、构造与析构、重载、继承和多态；
- 编译、链接、预处理、命名空间和类型别名等不阻塞算法主线的语言机制。

这里不能简单把“指针与引用”整体视为一个可有可无的主题。原生指针可以后移；引用
却会在函数间传递和修改 `vector` 等对象时成为真实需要。下一轮应把引用拆成两层：

1. 主线只讲引用是别名，以及引用参数怎样避免复制、允许修改调用方对象；
2. 进阶再比较引用与指针、引用折叠以外的 `const` 组合和更底层的对象模型。

同理，结构体的最小用法也不宜全部移走。本书后续会用 `struct` 封装边、线段树、
树状数组和其他具有持久状态与操作的数据结构；若主线从未定义结构体，这些代码就会
形成隐藏前置。主线只需先学字段、对象和点运算符，结构体指针、内存布局及完整类
机制可以后移。`pair` 能替代某些短记录，但不能成为回避具名状态和数据结构封装的
唯一方案。

### 数组保持独立

数组不是“地址、指针与引用”单元的附属物。它本身至少包含：

1. 元素类型、长度与容量；
2. 从 `0` 开始的原生下标和本书题目对象的下标转换；
3. 一维遍历；
4. 越界，包括负下标和超过右边界；
5. 无初始化器、`{}`、部分初始化列表等初始化方式；
6. 多维数组的行列下标；
7. 多维数组在常见实现中的连续、行优先排列规律。

数组可以作为函数实参。需要准确表述的是：在大多数函数形参声明中，数组形参会
调整为指向首元素的指针，因此函数不会从该形参自动得到第一维长度；通过数组引用
等写法仍然可以保留完整数组类型。主线数组文章不必提前展开这套类型规则，只需明确
“数组不能作为函数参数传递”是错误说法，并把细节交给后面的参数传递或指针扩展。

多维数组可以先通过地址相邻关系和下标公式理解连续排布，不要求读者已经会写指针。
指针文章以后再解释数组表达式、首元素地址和形参调整之间的关系。

### 初始化与存储期的分工

数组和基本类型文章先建立最安全的习惯：需要确定初值时显式初始化。内存与存储期
文章再解释为什么看似相同的声明会有不同结果：

- 没有初始化器的局部内置整数通常具有不确定值，读取它可能导致未定义行为；
- 没有初始化器的局部内置数组，其元素同样不会自动变成零；
- 具有静态存储期的标量和数组在其他初始化以前会先零初始化；
- 局部 `int x{};` 和 `int a[10]{};` 仍会得到零值，不能笼统说“局部对象不初始化”；
- `string s;` 和 `vector<int> a;` 会执行各自的默认构造，得到空对象；
- `vector<int> a(n);` 的 $n$ 个整数会值初始化为零，而 `reserve(n)` 只改变容量，
  不创建这 $n$ 个元素。

这里需要区分“内置类型没有初始化器时的默认初始化结果”和“类类型调用默认构造”。
“C++ 对象都会自动初始化”与“局部变量都不会初始化”都是过度概括。

### 第 02 章收缩为算法入门

第 02 章当前之所以像一个伪概念，是因为它既是方法总览，又已经展开排序、二分、
前缀和、双指针和贪心等完整解题单元。下一轮建议把路线阶段名暂定为“算法入门”，
只完成三项准备工作：

1. 复杂度；
2. 算法设计思想；
3. 标准库基础。

复杂度仍是完整单元，包含复杂度口径和从代码分析复杂度。均摊复杂度与势能法依赖
数据结构经验，继续留在高中阶段的复杂度进阶。

“算法设计思想”是一张以后学习的地图，不是把所有算法压缩成一篇模板合集。它用
极小问题区分：

- 枚举遍历候选；
- 模拟复现过程；
- 递归用较小问题定义当前问题；
- 分治把问题拆成相对独立的部分再合并；
- 贪心只保留当前能够证明安全的选择；
- 动态规划保存重复子问题并按阶段复用结果。

这一单元只要求读者认识每种思想解决问题的视角，并知道后面会分别展开。直接循环
枚举可以作为基线例子；子集、排列、组合和 DFS 枚举不在这里学习。分治只建立名称，
等快速排序和归并排序提供具体经验以后再完整推导。贪心也只建立“局部选择必须证明”
的警觉，不提前装入全部证明和题型。

“标准库基础”继续放 `vector`、`fill`、`pair`、`tuple`、`sort` 和 `unique`，
`array` 作为扩展。它们是进入真正算法单元以前马上要使用的表示和工具，不等于把
全部 STL 接口提前教完。

### 第 03 章承担第一次完整展开

排序、二分、前缀和等内容已经是能够独立解决一类问题的算法，不再勉强留在“算法
入门”。下一轮把它们与当前初中基础内容一起按依赖重组。候选顺序是：

1. 排序；
2. 分治；
3. 二分；
4. 前缀和；
5. 数组与链表等线性表；
6. 栈；
7. 队列；
8. 双指针；
9. 模拟的具体方法；
10. 贪心的选择、证明与典型构造；
11. 图、树和搜索；
12. DFS 枚举、排列、组合与子集；
13. 数论、动态规划与字符串等现有初中主干。

这份顺序仍然只是下一轮审查输入，不直接钦定最终表格。需要重点检查：

- 快速排序和归并排序先提供分治实例，再归纳完整分治思想；
- 排序先于二分和离散化；
- 前缀和只依赖数组，不必等待链表；
- 双指针放在线性表以后，可以自然比较数组相向指针、同向窗口和链表快慢指针；
- 普通循环枚举保留在算法入门，DFS 型枚举等学过状态空间和深度优先搜索再展开；
- 贪心完整单元移入第 03 章，但基础例子不依赖 `priority_queue`；优先队列仍只负责
  后续加速反复取最值；
- 递归的 C++ 调用机制属于第 01 章，递归式与问题分解可在算法入门建立直觉，DFS、
  分治和动态规划分别在自己的单元再次使用，不能重复成三套无关定义。

因此，第 02 章和第 03 章并非同一个概念：前者最终应是“怎样评价算法、有哪些设计
视角、马上需要哪些工具”，后者才是“逐类学习能够正式解决问题的方法与数据结构”。
若正式重排以后第 02 章只剩三个单元，这是职责收窄后的正常结果，不需要为了章幅
对称重新塞入具体算法。

## 第 01 章下一轮主题重组

以下内容记录 2026-08-16 讨论形成的下一轮审查方案。它暂时不替换后面的
“已确认结构”，也不表示正式学习路线已经完成迁移；其作用是完整保存新发现的
教学关系。等当前正文推进到合适边界以后，再用这些关系重排第 01 章的单元、文章
顺序和扩展归属。

学习路线中的单元和模块目录中的专题不必同名。前者回答“读者现在怎样连续
学习”，后者回答“复习某一类知识时去哪里查找”。正文也不再手工维护“上一篇”
或“下一篇”：同一篇文章在两套目录中的相邻文章可能不同，导航应由网站根据当前
目录自动生成。

### 入门

1. Hello World!；
2. A+B Problem。

这个单元只让读者第一次看懂、运行和提交一个程序。`int`、`cin`、`cout`、
`return` 和换行可以先按最小含义解释，完整语言规则留到真正需要时回访。单元名可
在“入门”和“第一个程序”之间继续审查，当前倾向使用简洁的“入门”，不把它写成
“前言”：两篇都有实际知识和练习，不是只介绍全书结构。

### 数值类型与算术

这个候选单元围绕“程序怎样保存并计算数值”组织，而不是先按语言规范列出全部
基本类型：

1. 整数类型；
2. 浮点类型；
3. 算术运算；
4. 数值类型转换；
5. 数学函数。

整数文章依次说明最常用的 `int`，再比较 `short`、`long`、`long long` 及其
`unsigned` 形式。正文区分 C++ 标准保证的最小范围与现代竞赛环境中的常见位宽，
不把某一平台的 `sizeof` 结果写成所有实现的唯一规定。

算术部分包含加、减、乘、除、余数、括号和复合赋值。必须明确：整数除法向零
截断；不同数值类型共同运算时会发生类型转换；复合赋值最后还要转换回左侧类型；
`^` 是按位异或，不是乘方。

数学函数介绍 `<cmath>` 中真正会用到的 `sqrt`、`pow`、`floor`、`ceil`、
`trunc` 和 `round`。`pow` 主要按浮点语义计算，不能当成保证精确的整数乘方；整数
指数通常用循环或后面的快速幂。向上取整除法要注明适用的符号范围和溢出边界，
不能把 `(a + b - 1) / b` 写成对任意整数都成立。

各类字面量在第一次学习对应类型时解释，不再要求一个早期“字面量”总览重复所有
写法。普通 `const` 在出现第一个需要保持不变的名字时引入；`constexpr` 作为扩展，
用于区分“不可修改”与“可在编译期求值”。`volatile` 只留在模块目录的扩展阅读：
它约束某些访问的可观察行为，常见于信号处理、内存映射设备等语境，不能充当线程
同步工具，在算法竞赛主线中的收益极低。

### 进制与机器表示

这个候选单元回答“同一串位怎样表示数值”，标题暂定为“进制与机器表示”，不缩成
“整数的二进制表示”：二进制也用于表示小数、字符和其他数据。

1. 位、字节与常用存储单位；
2. 二进制、八进制、十六进制；
3. 进制转换；
4. 有符号整数的机器表示；
5. 小数的二进制表示；
6. IEEE 754（附属扩展）。

八进制和十六进制首先被解释为二进制的紧凑写法，再介绍 C++ 的 `0b`、前导 `0`
和 `0x` 整数字面量。进制转换同时包含手算和程序实现：任意进制转十进制、十进制
转任意进制，以及数字字符与数值之间的转换。

原码、反码和补码属于初赛可能考查的机器表示知识，不能丢失；是否全部放在核心
路线，等拆篇时根据正文长度决定。小数部分区分定点表示和浮点表示；IEEE 754 继续
作为浮点类型的附属扩展，帮助解释行为但不要求理解或记忆所有字段。

这里第一次展示按进制输入输出，后面的输入输出单元再统一整理接口。必须区分
“用定点方式存储数值”和 `std::fixed`：后者只是要求流用固定小数形式输出浮点数，
不会把 `double` 改成定点数类型。

### 位运算

进制与机器表示建立“一个整数由若干位组成”以后，再独立学习位运算：

1. `&`、`|`、`^` 与 `~`；
2. `<<` 与 `>>`；
3. 读取、设置、清除和翻转某一位；
4. 最小的位掩码应用；
5. 当前真正需要的运算符优先级。

位运算只直接用于整型或枚举类型，并先经过整数提升，不能对 `float` 或 `double`
逐位调用这些运算符。正文同时回访整数宽度、无符号整数和溢出：无符号运算按
$2^N$ 取模；有符号溢出是未定义行为；C++17 中负的有符号整数右移结果由实现决定；
移位位数和移位结果也有自己的有效范围。混合多个运算符时以括号表达意图，不要求
背完整优先级表。

### 布尔、比较与逻辑

这一单元把互相直接依赖的三件事放在一起，但仍保留聚焦文章：

1. 布尔类型；
2. 比较运算；
3. 逻辑运算与短路求值。

`bool` 的两个值是 `false` 和 `true`；整数转换为布尔值时，零为假，非零为真。
`!!x` 只是先取反、再把结果取反，从而把任意条件规范成布尔值，不把它包装成需要
背诵的技巧。`sizeof(bool)` 至少为一个字节，主流环境通常也是一个字节，但这不代表
它具有 256 个逻辑取值，也不能据此断言标准唯一规定了它的物理编码。

比较运算必须专门指出 `1 <= x <= n` 在 C++ 中不会表达数学上的连锁不等式：第一
次比较先得到 `bool`，再拿它与 `n` 比较。正确写法是 `1 <= x && x <= n`。

逻辑运算解释 `!`、`&&`、`||` 和短路求值。短路可以在访问数组或解引用指针前检查
边界与空值；同时也意味着右侧表达式可能根本不执行，不能无意依赖其中的副作用。

### 控制流

1. `if` 与 `else`；
2. `switch`；
3. `while`；
4. `for`；
5. `do while`（低频或扩展）。

`break` 和 `continue` 在循环的实际语境中解释，不独立成篇；`break` 还要在
`switch` 中说明。自增、自减从语言分类看是表达式运算符，但第一次自然用途出现在
循环，因此学习路线放在这里解释，模块目录仍可把它们归入运算符专题。`return`
属于函数，不为了凑齐“改变执行顺序”而塞进控制流文章。

### 函数、调用栈与递归

函数主题需要覆盖：定义、调用、返回值、形参与实参、先声明后使用、函数原型和
互相递归。读者先理解普通函数调用需要保存哪些信息，再看嵌套调用和调用栈，最后
把递归解释为函数调用自己的特殊情况。

这一组最终可以根据实际篇幅拆成“函数”和“调用栈与递归”两个相邻单元，但不能把
调用栈提前成一张脱离函数的内存分区图。第 02 章中的“递归与递推”讨论算法怎样
通过较小问题定义当前问题，不再重复 C++ 函数语法。

### 数组，以及地址、指针与引用

数组先独立建立一维、多维数组的容量、长度、下标、遍历和连续存储。随后相邻学习：

1. 地址与字节寻址；
2. 指针；
3. 引用；
4. 参数传递；
5. 指针与引用中的 `const`。

指针与引用关系紧密，但不是同一个概念：指针对象保存地址，引用为已有对象提供
别名。学过二者以后再比较按值、指针和引用参数，不把“指针、引用和传参”压成一个
主题。

多维数组的行优先布局、作为函数参数时后续维度为什么必须已知，以及高维指针类型
放入数组或指针的附属扩展。大型内置数组放在全局作用域，是竞赛中避免调用栈大对象
与复杂参数声明的一种选择；需要在函数间传递并修改动态序列时，通常传 `vector` 的
引用。两种选择各有语境，不把其中一种宣布为所有代码的唯一规范。

### 字符与字符串

字符串建立在数组以后，并围绕“怎样表示和处理文本”形成完整单元：

1. 字符类型；
2. ASCII 与常用字符分类；
3. 转义字符；
4. C 字符串；
5. `string`；
6. 单词、整行与空白字符输入；
7. 字符和数值的相互转换。

字符也是整数类型，但具有文本输入输出、编码和转义等独立行为。正文需要准确说明
普通 `char` 的有符号性由实现决定，并区分 `char`、`signed char` 与
`unsigned char`。转义字符至少包括空字符 `\0`、换行 `\n` 和制表符 `\t`。

输入部分必须明确比较：`scanf` 的 `%c` 默认不跳过空白，`%s` 跳过开头空白后在
下一个空白处停止；格式化提取 `cin >> ch` 默认跳过空白，`cin.get` 不跳过；
`fgets`、`cin.getline` 和 `getline` 都能按各自接口读取整行。字符分类、大小写
转换、数字字符转数值和数值转数字字符也在这里出现；把数字字符串还原为整数可以
与进制转换形成自然的交叉练习。

### 名称、作用域、变量与常量

旧的“变量、常量与字面量”主题过于含混，下一轮按真实问题重新检查边界：

1. 标识符；
2. 关键字；
3. 变量；
4. 常量；
5. 作用域与名称遮蔽；
6. 命名空间、using 与类型别名（扩展）。

变量只需在入门程序中给出最小用法，等读者已经见过函数和代码块以后，再系统归纳
标识符、关键字、局部名称、全局名称与作用域。字面量跟随具体类型学习，不再为了
表格完整强行独立成单元。作用域回答名称在哪里可见，不能与对象存在多久混为一谈。

命名空间解决不同区域的同名问题；using 声明、using 指令和 `using` 类型别名是
三套语法；`typedef` 与 `using` 类型别名分别成篇。这些内容适合在已经能写程序后
作为扩展整理，不阻塞早期主线。

### 内存与存储期

这个单元在变量、函数、数组和地址都已经出现后，再统一解释：

1. 竞赛程序常见的内存区域；
2. 全局与局部对象的常见存放位置；
3. 调用栈与动态分配的关系；
4. 存储期；
5. `static` 局部变量；
6. 对象生命周期（扩展）；
7. 指针宽度、数据模型与机器字长（附属扩展）。

“高地址、低地址”和常见内存分区只作为具体实现示意，不能写成 C++ 标准规定的
唯一布局。`static` 局部变量的名称仍然只有块作用域，但对象具有静态存储期，并在
第一次执行到声明时初始化；不能把它叫作“局部作用域的全局变量”。

指针在常见 32 位和 64 位环境中通常分别占 4 字节和 8 字节，但这是平台数据模型的
常见结果。指针大小、某种整数大小、地址空间和机器字长彼此相关却不完全等同，正文
必须把它们作为不同概念解释。

### 输入输出

Hello World! 和 A+B Problem 先使用最小的 `cout` 与 `cin`。等读者已经认识整数、
浮点数、字符和字符串以后，再系统整理：

1. `scanf`；
2. `printf`；
3. `cin`；
4. `cout`；
5. 格式控制；
6. 文件输入输出与重定向。

`scanf` / `printf` 与 `cin` / `cout` 最终分篇。输入需要覆盖空白处理、失败状态和
常见格式说明符；输出需要覆盖精度、科学计数、`fixed`、宽度、填充、左右对齐以及
显式正号等常用状态。整行输入与字符、字符串单元交叉引用。关闭同步以后不能再随意
混用两套接口；低频格式标志只要求会查，不要求全部背诵。

进制输入输出和浮点格式会在对应类型第一次出现时先学习最小用法，再在这里统一
复习、比较和归纳。这是有意的螺旋式教学，不算重复错误。

### 结构体、类与从源码到程序

结构体在指针以后进入核心路线：结构体、成员、点运算符、结构体指针和箭头运算符
可以根据篇幅拆分。联合体和枚举各自是独立扩展，结构体内存布局是结构体的附属
扩展。模块目录可以把它们归到“复合类型”专题，但学习路线不宣称它们是 C++ 中
全部复合类型，也不因为目录分类而强迫合篇。

“类与对象”形成独立扩展单元，至少登记：类与对象、成员、封装、访问权限、友元、
构造函数、析构函数、函数重载、运算符重载、继承和多态。函数重载与运算符重载
分篇；虚函数、虚析构、对象切片以及虚函数表和虚表指针放在多态或析构的下级内容，
并明确虚表只是主流实现模型，不是标准钦定的对象布局。

“从源码到程序”继续作为另一个扩展单元，按编译、链接、预处理、`#include`、
`#define` 和条件编译拆篇。命名空间与类型别名可留在“名称与作用域”中，不为了
把所有低频语言机制装进一个扩展箱而破坏真实关系。

### 下一轮实施边界

1. 上面的标题表示候选单元，不表示每项只能写一篇；每篇仍只回答一个足够小的问题。
2. 保持必要前置：布尔值先于条件判断，条件与函数先于递归，数组先于 C 字符串，
   指针先于完整参数传递和箭头运算符。
3. 允许螺旋式回访：先用 `cin`、`cout` 和变量写出程序，后面再系统整理接口、名称、
   作用域和存储期；不能为了严格线性顺序把全部底层规则塞进第一周。
4. 严格区分语言对象与机器实现、数据的存储方式与输出格式、名称的作用域与对象的
   存储期。
5. 扩展内容不得成为后续核心文章的隐藏前置；它们可以完整而准确，但必须说明当前
   学习收益。
6. 正文不写依赖目录位置的“上一篇”或“下一篇”；需要回顾具体知识时直接引用文章名。

## 第 02 章当前重组基线

第 02 章继续使用已经确认的十个单元，并按下列顺序理解：

1. 复杂度；
2. 标准库基础；
3. 枚举与模拟；
4. 递归与递推；
5. 排序；
6. 分治；
7. 二分；
8. 前缀和；
9. 双指针；
10. 贪心。

复杂度是完整单元，不是短导论。它先讲输入规模、时间与空间复杂度、大 $O$、最坏
情况和常见数量级，再逐步分析线性变化、成倍变化、顺序代码、嵌套循环、依赖边界
和多个下标的总移动次数。$o$、$\Omega$、$\Theta$ 等渐近记号进入配套扩展；均摊
复杂度与势能法在高中阶段重新组成第二个复杂度单元。

标准库基础只放后续马上需要的 `vector`、`fill`、`pair`、`tuple`、`sort` 和
`unique`；`array` 是同层级扩展。`memset` 不是 STL，只在 `fill` 正文中辨析按
字节写入；自定义排序是 `sort` 的下级内容。`string` 留在第 01 章的字符与字符串
单元。`min`、`max`、`swap` 和 `reverse` 在第一次真实使用时解释。

枚举与模拟先建立算法思想，只讲直接循环枚举和按规则维护状态。子集、位掩码、排列
和组合等具体枚举移到第 03 章的深度优先搜索单元。递归与递推讨论用较小问题定义
当前问题，不提前引入记忆化搜索和动态规划。

排序依次包含冒泡、选择、插入、计数、快速、归并、排序性质比较和离散化；桶排序
与计数排序保持区别，基数排序和桶排序作为扩展，堆排序等学过二叉堆以后再回访。
快速排序和归并排序为分治提供具体经验，所以独立的分治单元放在排序之后、二分
之前。

二分单元包含普通查找、边界查找、标准库接口、整数答案二分和浮点数答案二分，并
集中处理闭区间、左闭右开接口、中点取整、溢出和终止条件。前缀和单元包含前缀和
与差分；双指针单元包含相向指针、同向指针与滑动窗口。

贪心先讲选择依据和正确性，再学习邻项交换证明与反悔贪心。反悔贪心的基础版本用
线性扫描寻找应撤销的选项；`priority_queue` 只是在后面学过二叉堆后，加速反复
取最值的标准库工具。

数组、链表、栈、队列和对应标准库接口属于第 03 章；二叉堆、哈希表、
`priority_queue`、有序关联容器和无序关联容器属于第 04 章，不回填到算法基础。

## C++ 基础的已确认结构

### 第一个竞赛程序

1. Hello World!
2. A+B Problem

### 基本类型

1. 整数类型
2. 浮点类型
3. 字符类型
4. 布尔类型
5. 类型转换。

类型转换区分隐式转换与显式转换，并说明截断和精度丢失。转换为 `bool` 时，零变成 `false`，所有非零值包括负数都变成 `true`；`bool` 的对象大小不表示它能保存同样数量的独立数值。

### 变量、常量与字面量

1. 变量
2. 常量
3. 字面量

每种类型在自己的文章中解释对应字面量的具体写法；“字面量”只统一名称、类型关系以及它与变量、`const` 对象的区别，不重复机器表示。

C++ 标准术语中，由声明引入的对象或引用是变量，因此 `const int MOD` 仍然是变量，同时也是 `const` 对象。文章标题和日常正文使用竞赛圈更自然的“常量”；首次定义时再说明标准分类，并区分字面量、常量和常量表达式。

C++ 中的对象不限于类实例；普通整数、数组和结构体实例都可以是对象。正文第一次使用“对象”时说明这个范围，后续日常表达仍可直接说变量。

`constexpr` 与 `volatile` 是本单元中的独立扩展。前者在普通常量以后区分“值不可修改”和“可在编译期求值”，后者只准确解释访问语义及其不能替代线程同步的边界；二者都可跳过，不作为后续核心文章的前置。

### 表达式

1. 算术运算符
2. 比较运算符
3. 逻辑运算符
4. 自增、自减运算符
5. 复合赋值运算符
6. 位运算符

位运算符文章覆盖 `&`、`|`、`^`、`~`、`<<` 和 `>>`；逻辑运算符文章覆盖 `&&`、`||` 和 `!`。不建立一篇要求背诵完整表格的运算符优先级文章：每篇只解释当前必须知道的局部关系，混合不同运算符时优先用括号直接表达结构。

### 控制流

1. `if` 与 `else`
2. `switch`
3. `while`
4. `for`
5. `do while`

`switch` 不与 `if` 合篇；`while`、`for` 和 `do while` 也各自成篇。`break` 在 `switch` 和循环的实际语境中解释，`continue` 在循环中解释，不单独成篇。`do while` 使用频率低，但语义独立，因此放在本单元最后，目标只是会读、会在“至少执行一次”时使用。

### 函数

函数不是为了凑“控制流”分类而并入条件和循环，也不与作用域机械组成一个单元。这个单元只回答怎样定义、调用函数以及怎样在调用两端传递最基本的信息：

1. 函数的定义与调用；
2. 返回值与 `return`；
3. 形参与实参；
4. 递归函数留到调用栈以后回访，不在函数基础中提前倾倒。

这里的形参与实参只建立调用语法和按值复制的最小直觉；指针参数、引用参数和完整的参数传递比较留到读者学过地址、指针与引用以后。

### 名称与作用域

这个单元回答“程序中的名称怎样写、在哪里有效，以及出现重名时怎样指定目标”。建议顺序为：

1. 标识符；
2. 关键字；
3. 作用域；
4. 命名空间与作用域解析运算符 `::`（扩展）；
5. using 声明与 using 指令（扩展）；
6. `typedef` 类型别名（扩展）；
7. `using` 类型别名（扩展）。

标识符文章先总结变量名、函数名和类型名怎样书写，并区分“语法上合法”与“名称能够表达含义”。关键字是语言预留并赋予语法作用的词，它只通过“不能拿来充当普通标识符”与前一篇相连，因此单独成篇。关键字文章回顾此前见过的 `int`、`return`、`if`、`else`、`for`、`while`，不要求背诵完整关键字表。

作用域在读者已经见过函数和代码块后学习。这里区分局部名称、全局名称以及遮蔽关系。命名空间建立命名空间作用域并解决不同库或代码区域的重名；using 声明引入一个名称，using 指令让未限定名称查找考虑一个命名空间，二者都不是类型别名。`typedef` 和 `using` 类型别名则为已有类型引入另一个名称。

作用域只决定名称在哪里可见，不决定对象存在多久。`static` 局部变量的名称仍然具有块作用域，但对象具有静态存储期，因此移到“内存与存储期”单元；命名空间作用域的 `static` 和类的静态成员等其他语义分别在链接与类的语境中解释，不把多个规则压成一句“静态变量”。

### 数组

1. 一维数组
2. 多维数组

一维数组先讲容量、下标和遍历；多维数组再讲行列下标。数组与字符串不共用学习单元：数组解决一组同类型对象怎样连续存放，字符串还要建立终止标记、文本输入和字符串操作等独立语义。

`std::array` 不进入这个单元。它没有增加理解内置数组所必需的概念，而是把编译期固定长度序列包装成具有标准容器接口和值语义的类型，因此作为带星文章放入第 02 章“标准库基础”，与 `pair`、`tuple` 处于相同的课程层级。

### 字符串

1. C 字符串；
2. `string`；
3. 整行输入。

C 字符串建立在字符数组之上，先说明末尾空字符。随后从手工维护长度和容量的困难引入标准库类型 `string`，明确它虽然属于 C++ 标准库，但在这里按“怎样表示文本”的问题进入主线，不需要先学完其他 STL 接口。整行输入最后统一比较 `fgets`、`cin.getline` 和 `getline`，不能误写成只有 `string` 才能读整行。

正文需要区分头文件 `<cstring>` 与 `<string>`：前者提供处理 C 字符串的函数，后者声明标准库字符串类型。

多维数组的连续布局、行优先顺序、作为函数参数时后续维度为什么必须已知，以及结构体内存对齐都属于明确的配套扩展。正文要解释：大型内置数组常放在全局作用域是一种避免调用栈大对象和复杂参数类型的竞赛实现选择；需要传递并修改动态序列时，通常传 `vector` 的引用。这不是宣称所有代码都应使用全局静态数组。

### 地址、指针与引用

建议逐篇组织：

1. 地址与字节寻址；
2. 指针；
3. 引用；
4. 参数传递；
5. 指针与引用中的 `const`。

地址先说明变量占据内存、地址用来定位它，不需要一开始讲完整计算机体系结构。指针保存地址，引用提供别名；二者有联系但不能合成一个主题。参数传递在读者已经理解值、地址和引用以后，区分按值复制、指针参数和引用参数。

早期“常量”文章只讲普通 `const` 对象。`const int*`、`int* const`、`const int* const` 和 `const T&` 等读者理解指针与引用后再系统比较；不能让早期常量文章同时承担对象、指针和引用三套声明语义。

### 结构体、联合体与枚举

建议逐篇组织：

1. `struct`；
2. 结构体成员与点运算符；
3. 指向结构体的指针与箭头运算符；
4. `union`（扩展）；
5. `enum`（扩展）。

把这一单元放在指针以后，`.` 与 `->` 都能从对象和地址的区别自然解释。中文正文使用“结构体”或“结构体类型”，不把 `struct` 固定称为“记录类型”。严格分类上，C++ 的 `struct` 与 `class` 都能声明类类型；主线先学习竞赛中把若干相关字段组织在一起的结构体用法。`union` 与 `enum` 在当前阶段适合了解但不会阻塞后续，因此与结构体同单元并标星；成员函数、继承和多态进入独立的“类与对象”单元。

结构体内存布局使用 `*0121e1` 作为 `struct` 的附属扩展，不进入学习路线或扩展阅读索引；模块目录仍完整收录，并从结构体正文提供直接链接。

内存布局扩展说明字段声明顺序、对齐、填充、`sizeof` 和二维数组嵌入结构体时的布局。它帮助解释观察到的大小，不能把某个编译器的具体填充结果写成所有平台唯一可能的标准布局。

### 内存与存储期

这一单元在变量、函数、地址和数组都出现以后再学习：

1. 竞赛程序常见的内存区域；
2. `static` 局部变量；
3. 对象生命周期（扩展）。

“内存区域”只从 C++ 竞赛程序解释全局或静态存储、调用栈和动态分配等常见现象，高地址低地址只作为实现示意，不宣称标准规定了唯一布局。对象生命周期、存储期和作用域是相互关联但不同的概念；需要分别命名，不能用一张“四区图”替代全部语言规则。

`static` 局部变量单独解释块作用域、静态存储期、首次经过声明时初始化以及跨调用保留状态。这里不扩张到内部链接和静态类成员；这些拼写相同但语境不同的规则由后续文章各自解释。

### 调用栈与递归

1. 调用栈；
2. 递归。

调用栈先解释一次普通函数调用保存什么，再解释嵌套调用，最后才引出递归。递归是函数调用自己的特殊情况，不把它提前放进与函数无关的章节。

### 输入输出

基本 `cin`、`cout` 已在 Hello World! 和 A+B Problem 中先作为格式代码使用。后期再分别系统整理：

1. 标准输入；
2. 标准输出；
3. 浮点精度和其他格式控制；
4. 整行输入；
5. 文件重定向。

这一安排允许读者先写程序，再理解 `scanf` / `printf`、流状态和格式接口，不为了介绍完整 I/O 提前阻塞语言主线。文件重定向介绍 `freopen` 和 OI 赛制中按题目要求读写文件的标准写法。

### C++ 扩展阅读

扩展内容也必须保留明确文章边界和教学问题，不能只留一串名词。

#### 从源码到程序

建议逐篇组织：

1. 编译：C++ 源码怎样翻译成机器可以执行的内容；
2. 链接：多个翻译单元以及库中的名称怎样组成最终程序；
3. 预处理；
4. 预处理：`#include`；
5. 预处理：`#define`；
6. 条件编译。

预处理正文先给出从源文件到可执行程序的阶段关系，再解释预处理命令发生在哪里。`#include` 单独比较尖括号与双引号的查找语义。宏覆盖对象式宏、带参数宏、括号与作用域污染等问题，并简短解释多语句宏为什么常由 `do while` 包裹；正文可以展示 `FOR`、`REP` 等竞赛代码中可能遇到的缩写，但明确本书不采用这种风格。

这里只建立读代码和理解错误信息所需的直觉，不扩展成完整编译原理课程。`#include` 与 `#define` 都由预处理器处理，但用途不同，因此分别成篇。宏需要说明文本替换、参数括号和污染范围等陷阱，同时明确本书模板不用宏压缩类型、常量、左右儿子或递归参数。条件编译同样属于预处理，但它包含 `#if`、`#ifdef`、`#ifndef`、`#elif`、`#else`、`#endif` 和 `defined(...)` 等一整套指令，并承担本地调试开关、include guard 与多构建配置等独立问题，因此单独成篇，标题使用概念名“条件编译”，不用其中某一条指令 `#ifdef` 代替整篇主题。

#### 命名空间、using 与类型别名

这些文章在正式学习路线中与“标识符”“关键字”和“作用域”共同组成“名称与作用域”单元；这里继续记录各篇文章的边界：

建议顺序：

1. 命名空间与作用域解析运算符 `::`；
2. `using namespace std;` 与 using 声明；
3. `typedef`；注意提一下这个是安全的，比define好，不过define顺序是反过来的。
4. using 类型别名。

命名空间先解释它怎样解决不同库或区域中的同名问题，以及 `std::vector` 中 `::` 的作用。随后才能准确解释 `using namespace std;` 为什么让代码省略 `std::`，以及只引入一个名称的 using 声明。

using 声明、using 指令和 `using` 类型别名共享同一个关键字，但属于三套语法：`using std::cin;` 引入一个名称，`using namespace std;` 让未限定名称查找考虑整个命名空间，`using ll = long long;` 才声明类型别名。正文不能因为拼写相同就把前两者称为类型别名。

`typedef` 和 `using` 类型别名解决相似问题，但语法与现代模板能力不同，因此分别成篇。先用 `typedef long long ll;` 解释本仓库实际采用的别名，再让 `using` 类型别名文章比较等号两侧的顺序和别名模板；不能只写一个泛化的“类型别名”条目后遗忘差别。

#### union 与 enum

`union` 和 `enum` 分别成篇，不把枚举错误地归为“结构体的一种”。`union` 解释多个成员共享存储以及当前有效成员的约束；`enum` 解释一组具名整数值和作用域枚举。二者在竞赛中都低频，进入扩展阅读，但初赛或阅读代码可能遇到，因此需要准确的最小正文。

#### 类与对象

建议形成一个独立扩展单元：

1. `class` 与对象；
2. 类的成员；
3. 访问权限，其中介绍友元（`friend`）；
4. 构造函数；
5. 析构函数；
6. 函数重载与运算符重载；
7. 继承；
8. 多态。

`class` 首先与已经学过的 `struct` 比较：两者都能声明类类型，关键的语言差别包括默认成员访问权限和默认继承权限，而不是“`struct` 只能保存数据、`class` 才能有函数”。随后定义对象是类类型的实例之一，同时提醒 C++ 标准中的“对象”范围更广，普通整数同样是对象。

“类的成员”区分成员变量与成员函数，并说明日常所说的方法对应成员函数。还需要区分属于每个对象的非静态成员与属于类本身的静态成员；常量成员仍然是成员变量，只是具有 `const` 限制，不能另造“成员常量不是变量”的错误分类。

访问权限至少覆盖 `public`、`private` 和 `protected`，不能因为竞赛中前两者更常见就漏掉第三种。`friend` 允许指定函数或类访问非公开成员，是访问控制的例外机制，不把它描述成一种访问权限关键字。

构造函数解释对象建立时的初始化；析构函数解释对象结束时的清理。虚析构函数放在多态语境中说明：通过基类指针删除派生对象时为什么基类析构函数需要是虚函数，而不是把“析构函数一定要 virtual”写成普遍规则。

继承说明派生类复用和扩展基类接口，并覆盖单继承、多继承以及公开、保护、私有继承的名称可访问关系。竞赛主线不依赖这些机制，但正文不能只列名词。

多态重点区分：

- 同名函数或运算符根据静态参数选择属于重载；
- 派生类重新定义基类虚函数属于覆盖；
- 通过基类指针或引用调用虚函数，运行时选择实际派生对象的实现，才是这里重点解释的运行时多态。

正文可用最小示例辅助理解虚函数、对象中的实现开销以及常见的虚函数表模型，但必须注明虚函数表和虚表指针是主流实现技术，不是标准强制规定的对象布局。按值把派生对象复制到基类对象会发生对象切片，不能借此展示运行时多态。

这些机制竞赛中收益很低，扩展单元只要求理解名词、基本行为和阅读能力，不扩张到工程设计模式、模板元编程或完整现代 C++ 工程实践。

#### 其他语言机制

`inline`、`volatile`、对象生命周期、`constexpr` 等按独立问题标星，并放入语义最接近的学习单元和扩展阅读索引。每篇必须说明真实语义以及为什么不阻塞竞赛主线；不能因为作者当前很少使用就写成“没有作用”，也不能为了显得完整把它变成后续核心文章的前置。

## 算法基础的已确认结构

### 学习单元一：复杂度

1. 复杂度
2. 从代码分析复杂度
3. 渐近记号（配套扩展）

“复杂度”建立输入规模、时间与空间复杂度、大 $O$、最坏情况、常见增长数量级、总空间与额外空间。“从代码分析复杂度”逐步分析：

- `i++`、`i += c` 等线性变化；
- `i *= c`、`n /= c` 等对数变化；
- 顺序代码相加；
- 独立嵌套循环相乘；
- 内层边界依赖外层时求总次数；
- 多个下标互相影响时统计整个程序中的总移动次数；
- 数组、容器和递归调用栈的空间。

复杂度示例优先写能直接体现规模变化的 `n /= 2`，不为了炫示位运算改成把移位赋值塞进条件。渐近记号扩展完整区分 $O$、$\Omega$、$\Theta$、$o$ 和 $\omega$；核心路线不要求记忆大 $O$ 以外的形式化定义。

这个单元虽然位于第 02 章开头，但不是一篇短导论。“复杂度”负责尺度、记号和空间口径，“从代码分析复杂度”负责把代码翻译成执行次数；二者共同完成一次完整学习任务。递归式、均摊分析等需要后续结构的内容不为追求一次讲完而提前加入。

### 高中阶段的复杂度进阶

高中基础再次设置独立学习单元：

1. 均摊复杂度；
2. 均摊复杂度：势能法。

第一篇从一串操作的总成本出发，使用动态数组扩容、栈的连续操作等例子说明：某一次操作可以很贵，但任意操作序列的总成本仍然受到更紧的限制。正文依次介绍聚合法和记账法，并严格区分：

- 最坏复杂度研究最坏的一次或最坏输入；
- 平均复杂度依赖输入分布；
- 期望复杂度依赖随机变量；
- 均摊复杂度不需要概率，它对操作序列的总成本作保证。

势能法独立成篇。先为数据结构状态定义非负势能，再把一次操作的实际代价与势能变化相加得到均摊代价；最后对整个操作序列求和，解释中间势能怎样抵消。正文必须从聚合分析已经得到的总成本反推一个自然势能，不能一上来凭空给出公式要求背诵。

均摊复杂度不是“把最坏时间除以操作次数”，势能也不是程序中实际存储的能量。两篇都要通过同一个操作序列逐步计算实际代价、账户余额或势能变化，让三种证明方式得到一致结论。

### 标准库基础

不再设置一章按标准库名字平铺的“STL 工具大全”。当前马上需要的类型和算法组成“标准库基础”，其余接口在对应实际问题出现时学习：

1. `vector`；
2. `fill`；
3. `pair`；
4. `tuple`；
5. `array`（扩展）；
6. `sort`；
7. `unique`。

复杂度独立组成第一个学习单元，不属于标准库基础。读者理解操作次数以后，再把以上七项作为后续代码马上需要的标准库工具学习；这一单元不提前罗列全部容器。数组作为数据结构的访问、插入删除和复杂度放到初中基础，与链表连续比较，不在复杂度和标准库基础中间突然插入一个“抽象数组”单元。

`string` 跟在 C 字符串以后留在 C++ 基础。它在那里解释字符串值、`getline`、`substr`、查找和修改；字符串算法以后复用这些接口，不在排序前把 `string` 当作一个无动机的 STL 名称重新学习。

`pair` 与 `tuple` 在 `sort` 以前学习。两篇分别保持聚焦：`pair` 处理两个值，`tuple` 推广到数量固定的多个值；它们都说明默认字典序、结构化绑定以及字段缺少业务名称的代价。这样排序、去重、二分和带权图可以复用同一种复合值表示，不必在存图时临时插入标准库接口。

`array` 同样放在 `sort` 以前，但保持扩展标记。它是固定长度、同类型元素的标准库序列容器，支持值复制、返回整个对象和统一的容器接口；从课程层级看更接近 `pair`、`tuple` 等标准库值类型，而不是理解一维、多维内置数组的前置知识。竞赛中运行期长度优先使用 `vector`，简单编译期数组又常直接使用内置数组，因此读者可以跳过 `array` 而不阻塞后续主线。

`unique` 必须说明它只整理相邻等价元素并返回新的逻辑结尾，不会自行缩短容器；排序去重再组合 `sort`、`unique` 和 `erase`。

`fill` 是文章的唯一主题。`memset` 不是 STL，而是按字节写内存的 C 标准库函数；它只在 `fill` 正文的下属二级标题中用于辨析。普通数值数组优先使用 `fill`。必须展示 `memset(a, 1, sizeof a)` 不会把每个 `int` 赋成 `1`，并且不能把主流竞赛环境中常见的字节模式行为误写成适用于任意类型和平台的通用赋值规则。

`min`、`max`、`swap`、`reverse` 等接口在第一次真实使用时解释，不为追求形式对称全部独立成篇。

`sort` 是一篇完整文章。它先解释迭代器范围、默认升序、原地修改和简单类型，再用“自定义排序”二级标题逐步覆盖降序、多关键字、具名比较函数、lambda、函数对象、类型自己的 `operator<`、`stable_sort` 以及比较规则一致性。比较器离开排序没有独立学习目标，因此不另建目录节点；正文仍可用多个二级标题控制篇幅。

### sort 的默认顺序与自定义排序

C++ 已经为许多标准类型提供自然的默认顺序：

- `pair` 先比较 `first`，相等时比较 `second`；
- `tuple` 按元素位置从前到后比较；
- `array`、`vector` 和 `string` 按元素字典序比较；变长序列的共同前缀完全相同时，较短者更小；
- 对一个 `string` 自身调用范围排序，是重新排列其中的字符；把若干 `string` 作为元素排序，才使用字符串之间的字典序。

短小记录若所有字段都按同一方向组成完整字典序，优先复用 `pair`、`tuple` 或 `array` 的默认比较，排序、相等判断和二分更容易保持一致。例如把 `(value, id)` 作为整体按值、再按编号排序时，`pair` 比额外定义比较器更短。

这不是要求把所有记录压成匿名元组。字段具有明确含义、字段类型不同且数量较多、排序方向混合，或者同一种记录需要多套顺序时，具名 `struct` 更可读：

- 类型只有一种真正自然、会被排序和二分反复使用的顺序时，可以为类型定义严格的 `operator<`；
- 某次操作需要临时顺序时，向 `sort`、`lower_bound` 等算法显式传入同一个比较器，避免把临时规则伪装成类型的天然含义；
- 教学代码先使用命名比较函数；lambda 适合只在调用点使用的短规则；重载 `operator()` 的函数对象用于需要携带配置或复用的比较器，并在正文说明 `greater<T>` 本身就是这种对象。

多关键字比较必须逐层处理：主关键字不同就立即决定顺序，主关键字相等才看次关键字。比较器返回“左侧是否严格在右侧以前”，相等时必须为 `false`，不能使用 `<=` 或 `>=`。

降序需要比较三种写法：

1. 简单类型使用 `greater<T>()`；
2. 复杂或混合方向使用自定义比较器；
3. 先升序 `sort` 再调用 `reverse` 可以得到整体逆序，但 `reverse` 只是翻转已有顺序，还会同时反转所有关键字方向，并额外线性扫描一次。

这里的方法名是 `reverse`，不是只预留容量的 `reserve`。若要求“分数降序、编号升序”，不能把默认升序整体 `reverse`，必须明确写出混合方向的比较规则。

排序、去重和二分必须共享同一套“先后”与“等价”语义。若 `comp(a,b)` 是严格比较，则排序意义下等价表示：

```text
!comp(a, b) && !comp(b, a)
```

但 `unique` 默认调用 `==`，并不会自动从 `comp` 推导等价关系。自定义类型按部分字段排序、却按全部字段判断 `==` 时，排序去重可能不符合题意；应当定义相符的 `operator==`，或显式传入等价判断。使用默认 `pair`、`tuple`、`array` 的优势之一，就是完整字典序和完整相等关系已经配套；若题目只按其中部分字段分组，仍需单独定义等价条件。

### 初中基础的数据结构

最初接触的数据结构不再挤进一个大单元，而是按真实关系拆成三个学习单元。

“数组与链表”比较线性表的两种典型存储思路：

1. 数组；
2. 链表。

数组文章复用已经学过的 `vector`，重点讨论随机访问、插入删除和连续存储的复杂度。链表文章依次解释结构与遍历、插入删除、头指针与虚拟头节点、双向链表和环形链表。低频的标准库 `list` 留在模块目录，不作为主线默认选择。

“栈”单元包含：

1. 栈；
2. `stack`；
3. 出栈序列判定；
4. 表达式求值。

中缀、后缀表达式和表达式树都服务于表达式求值。正文只借树形结构说明运算符与操作数的从属关系，不提前要求读者掌握一般树算法。

“队列”单元包含：

1. 队列；
2. `queue`；
3. 双端队列；
4. `deque`。

双端队列是把普通队列允许操作的端点扩展到两端，不与队列并列为单元主题。`deque` 是具体序列容器，`queue` 是默认可由 `deque` 承载的容器适配器；两篇必须分别解释。两个栈实现队列可以作为队列文章的练习或应用，不为它单独建立路线节点。

单调栈和单调队列需要读者理解栈、队列、双指针式的单调移动和更完整的复杂度分析，继续放在高中基础。

链表统一使用以下名称，避免“头节点”一词在不同资料中的歧义：

- `head` 是指向或记录第一个真实节点的头指针、头下标；
- 第一个真实节点仍然保存题目数据；
- 虚拟头节点（dummy head 或 sentinel）是不保存题目数据的额外节点，位于第一个真实节点以前；
- 使用虚拟头节点可以让在链表开头插入、删除与其他位置共用相同操作，但不是链表定义所必需。

链表正文需要对比数组：数组能按下标 $O(1)$ 随机访问，但在中间移动大量元素代价较高；链表只有在已经找到相关节点时，修改链接才是 $O(1)$。不能只写“链表插入删除是 $O(1)$”而漏掉定位节点可能需要 $O(n)$。

栈、队列和双端队列是抽象数据结构；`stack` 与 `queue` 是容器适配器，`deque` 是具体序列容器。文章必须在概念与标准库实现之间维持这一边界。

`priority_queue` 不进入本单元。它在理解二叉堆以后进入初中进阶路线。递推也不再作为算法基础的第二篇；等到递归、数列关系或动态规划真正需要时再安排。

### 排序

主线建议顺序：

1. 冒泡排序；
2. 选择排序；
3. 插入排序；
4. 计数排序；
5. 快速排序；
6. 归并排序；
7. 排序方法比较；
8. 排序的应用：离散化。

前三种都是 $O(n^2)$ 的比较排序，但必须分开推导：冒泡排序不断消除相邻逆序，选择排序每轮找出剩余部分的最小值，插入排序把新元素插进已经有序的前缀。现有“基础排序”正文可以作为共同引言，但正式重组时应根据篇幅拆成聚焦的小文章，不能因为复杂度相同就混成一种方法。

计数排序与桶排序不是同一个算法。本书统一采用严格区分：

- 计数排序为每个离散整数值统计出现次数，再根据次数恢复顺序；
- 桶排序先把一个值域划分为若干桶，还需要处理每个桶内部的顺序；
- 有些 OI 资料口语上会把计数排序称为“桶排序”，正文首次出现时说明这种叫法，但后续不混用。

快速排序和归并排序各自独立成篇。它们不仅要给最终模板，还要逐步解释划分或合并操作、递归区间为什么缩小、终止条件和复杂度来源。归并排序需要说明它为什么天然适合链表；快速排序和堆排序依赖随机访问，在链表上不自然。这里只建立数组与链表排序方式的比较，不要求主线实现完整的链表归并排序。

`sort` 与 `unique` 在前面的标准库基础单元首次学习。离散化不是学习这两个接口的地方，而是第一次把“排序、去重、二分定位”组合成完整算法，因此放在排序单元末尾作为排序的典型应用。正文保留本书自定义数据从 $1$ 开始和闭区间的约定，并明确传给 STL 时怎样转换为左闭右开迭代器范围。

“排序方法比较”独立总结时间、额外空间、原地性、稳定性、是否依赖值域以及适合的存储结构。稳定性必须绑定到具体实现，不能写成“除选择、快速和堆排序以外都稳定”：

- 本书采用严格比较的冒泡排序和插入排序，它们稳定；
- 朴素选择排序、通常的原地快速排序、堆排序、希尔排序以及 `std::sort` 不稳定；
- 归并时相等元素先取左半边，归并排序才稳定；
- 计数排序若只按频次重建整数序列，不保存相同键对象的原顺序；使用前缀位置和额外输出数组才能实现稳定版本；
- 基数排序要求每一趟按位排序稳定，整个算法才能正确保持前面各位建立的次序；
- 桶排序是否稳定取决于元素入桶、桶内排序和拼接方式。

堆排序与基数排序各写一篇扩展阅读：它们确实有方法价值，但不阻塞主学习路线。希尔排序只在“其他排序”中给出名称、基本思想和复杂度难以统一表达的原因，不投入一篇主线教程。桶排序先在计数排序中辨析概念；以后只有在确实需要展示值域分桶方法时再扩成独立文章。

### 二分

建议顺序：

1. 二分查找；
2. 二分边界；
3. 标准库二分查找；
4. 整数答案二分；
5. 浮点数答案二分。

二分查找先手写“是否存在并返回任意一个位置”，建立闭区间候选与每轮排除一半的循环不变量。二分边界再分别推导第一个不小于目标、第一个大于目标以及由二者得到的最后一个不大于目标，不能在第一次遇到相等值时提前返回。

自定义代码统一使用从 $1$ 开始的下标和闭区间 $[l,r]$。标准库二分查找单独整理左闭右开迭代器接口：

- `binary_search` 只回答目标是否存在，不返回位置；
- `lower_bound` 返回第一个不小于目标的位置；
- `upper_bound` 返回第一个大于目标的位置；
- `equal_range` 同时返回上下界，二者之差就是等价元素数量。

两套区间约定都要会，但同一个手写循环中不能来回切换。普通竞赛代码优先调用标准库边界接口；手写版本的价值是建立不变量，并为后面的答案二分服务。

对同一有序范围，`upper_bound` 与 `lower_bound` 返回位置之差就是等价元素数量。普通二分查找、边界查找和答案二分各自有独立目标，因此分别成篇但共用一个学习单元；整体二分、并行二分与三分不进入这个基础单元。

所有 STL 二分接口都要求范围已经按照与查询一致的顺序排列。默认 `pair`、`tuple`、`array`、`vector` 和 `string` 可以直接复用自然字典序；自定义 `struct` 若定义了自然 `operator<`，可以构造同类型查询值。若排序时显式传入了比较器，二分时也必须传入兼容的比较器，不能先按一种顺序排序再按另一种顺序查找。

边界文章必须让每一行来自循环不变量，而不是要求背诵互相矛盾的模板。至少解释：答案仍在哪个区间、`mid` 满足条件后保留哪一半、区间怎样严格缩小、循环结束时哪个变量才是答案，以及不存在答案时怎样表示。

中点计算与 C++ 整数规则需要集中辨析：

- `l + (r - l) / 2` 避免直接计算 `l + r` 可能产生的溢出；
- C++ 有符号整数除法向 $0$ 截断，并不总是数学上的下取整；二分中 `r - l` 非负，因此除以 `2` 才与下取整相同；
- 非负整数右移一位等价于除以 `2` 后下取整，但 C++17 中负有符号整数右移的结果由实现决定；
- `+` 的优先级高于移位运算符，所以 `l + r >> 1` 会被解析为 `(l + r) >> 1`，但它仍然可能溢出且可读性差，本书不采用这种写法；
- 当更新形式为 `l = mid` 时需要使用上中点 `l + (r - l + 1) / 2`，否则两个相邻整数可能使循环不再缩小。

整数答案二分围绕“答案是否可行”具有单调性展开：先写判定函数，再决定要找第一个可行还是最后一个可行，最后把边界模板翻译出来。它不是在数组中找元素，因此与普通二分查找分篇。

浮点数答案二分单独成篇。它没有整数区间相邻时的自然终止点，竞赛模板优先采用固定次数迭代；若使用误差阈值，则必须结合题目要求解释绝对误差、相对误差以及循环条件。不能把整数模板简单换成 `double` 后继续依赖 `l + 1 < r`。

### 前缀和

主线只包含两篇：

1. 前缀和；
2. 差分。

差分是前缀和的逆过程，因此单元以“前缀和”为主题，差分作为掌握累计与恢复关系以后自然得到的第二篇。它们依赖数组，但不属于线性数据结构。数组文章回答元素怎样连续保存、怎样访问以及各种操作的复杂度；本单元回答怎样利用相邻位置的累计关系，把一批区间查询或修改预先合并。因此不把它们塞进“线性结构”单元，也不为了凑篇数与排序或二分合并。

两篇文章组成一对相反过程：

- 前缀和从原数组累计得到前缀数组，并用两个前缀的差回答静态闭区间和；
- 差分保存相邻元素的变化，在两个区间边界记录增量，再用一次前缀和恢复最终数组。

正文需要把这种对应关系画出来，但教学顺序仍然先前缀和、后差分。差分文章可以直接引用读者已经理解的前缀恢复，不重新发明一套不相干的记号。

主线边界是“一维数组、静态区间和”和“一维数组、离线区间加”。以下内容作为本单元的配套扩展分别登记，不塞回两篇基础正文：

- 二维前缀和；
- 二维差分；
- 前缀异或等把加减换成其他可逆运算的变化；
- 高阶差分；
- 树上差分，它依赖树的遍历与最近公共祖先，应留在图论路线；
- 差分约束，它是图上的不等式系统，与差分数组只有名称相似，不属于本单元。

前缀和与差分都使用从 $1$ 开始的下标和闭区间。`prefix[0] = 0` 与 `difference[n + 1]` 都是有明确语义的边界位置，正好体现本书愿意多留少量空间来消除特判的代码风格。

工具选择必须在本单元末尾说清：普通前缀和适合不修改的数组；普通差分适合全部修改完成后再统一恢复。若修改和查询必须在线交错处理，才进入后面的树状数组或线段树，而不是继续硬套这两个静态技巧。

### 双指针

主线包含两篇：

1. 双指针；
2. 滑动窗口。
它们操作数组或字符串中的下标，但仍然不是线性数据结构。线性结构说明数据怎样保存；双指针说明怎样让多个下标单调移动，从而一次排除一批候选。这个区别与前缀和、差分相同，因此单独组成一个短学习单元。

“双指针”先使用有序数组两数之和建立最基础的相向模型：左、右指针分别从两端开始，根据当前和的大小证明可以排除哪个端点。文章必须强调，关键不是代码中恰好有两个下标，而是每次移动都能证明被跳过的状态不会再成为答案。

同篇可以简短指出另外两种形态，但不展开成模板合集：

- 归并两个有序序列时，两个指针分别在各自序列中同向移动；
- 链表的快慢指针用于找中点或判环，依赖链表及环的具体性质，应当在对应问题中重新推导。

“滑动窗口”是同向双指针中专门维护连续区间的一类方法，因此它与双指针同单元但独立成篇。正文同时讲固定长度窗口和由条件决定长度的可变窗口，并让窗口状态只在端点进入或离开时增量更新。

基础示例使用非负或正数数组，使“扩展右端时窗口和不减、收缩左端时窗口和不增”的单调性成立。文章必须给出含负数的反例，说明滑动窗口不是看到连续子数组就能套用的语法模板；需要先证明左右端都不会回退。

复杂度统一从总移动次数推导。即使实现为外层 `for`、内层 `while`，只要左右端各自最多移动 $n$ 次，总时间仍然是 $O(n)$，不能只按循环嵌套层数误判为 $O(n^2)$。

三数之和、去重计数、多序列指针、回文扩展、链表判环以及带单调队列的窗口分别留在具体题型或后续数据结构中。基础单元只建立“单调移动并安全排除”与“维护连续窗口”两种可复用思想。

### 枚举与模拟

主线包含：

1. 枚举；
2. 模拟。

枚举与模拟确实属于算法基础。枚举先确定候选对象、唯一表示、生成顺序和检查成本；模拟先确定状态、每种操作的唯一效果以及执行顺序。它们不依赖某个高级数据结构，也是推导和验证后续算法时最可靠的朴素基准，因此可以组成一个相邻的基础单元。

二者仍然是独立文章。枚举解决“怎样不重不漏地遍历候选”，模拟解决“怎样逐条复现题目过程”，不能因为都常被称为暴力方法就混成一篇。

第 02 章只建立直接循环枚举的共同框架。子集与位掩码、排列和组合都需要进一步讨论状态表示、去重和生成顺序，移到第 03 章“图论：深度优先搜索”单元，与状态空间、隐式图和回溯共同学习；不能因为其中一种实现也能写成循环，就把具体枚举题型重新塞回基础概念文章。

分治不继续与枚举、模拟和贪心共同放在“基本算法设计方法”大表中。它依赖递归，并且已经通过快速排序和归并排序获得具体经验，应独立形成后续的“分治”单元。

## 贪心的基础与进阶应用

贪心是一种基础算法设计思想，不能因为某一种贪心依赖堆，就把整个主题推迟到初中进阶。算法基础中的“贪心”单元放在排序之后，包含：

1. 贪心选择与正确性证明；
2. 邻项交换证明；
3. 反悔贪心。

反悔贪心本身不依赖二叉堆或 `priority_queue`：基础版本可以在线性扫描中找出当前方案里最应撤销的一项。它因此留在第 02 章的贪心单元，与选择规则和证明方法连续学习；以后学过 `priority_queue`，再回看怎样加速反复取最大值或最小值。哈夫曼编码还依赖前缀编码和二叉树，放在初中进阶的“编码”单元，并回链到基础贪心的证明方法。

现有“选择与证明”使用最多不重叠区间，讲清一般交换论证：把某组最优答案中的第一项换成当前贪心选择，并证明合法性与答案质量不变。它保留为单元入口。

“邻项交换”是更专门的排序型证明，不能与一般交换论证只写成一个名词。若一个排列中相邻两项违背目标比较规则，就证明交换这两项不会让答案变差；反复交换可以消除全部逆序，从而得到按该规则排序的最优解。正文必须从两项对总目标的局部贡献推导比较式，而不是先猜一个 `sort` 比较器再补故事。

完整证明还必须补上从局部交换到全局顺序的桥梁：只要序列没有按照目标次序排列，就至少存在一对相邻逆序；交换它们以后答案不变差，并且逆序对数量严格减少。因此这一过程会在有限次交换后终止于目标全序。比较关系还必须具有一致、可传递的顺序语义；若局部比较会形成循环，就不能由相邻交换推出统一的全局排序。

文章名保留“邻项交换证明”和中文竞赛圈通行的“反悔贪心”，不为了标题表面对仗改成“邻项交换策略”和“后悔策略”。前者是证明与比较规则的推导方法，后者是维护、撤销或替换当前方案的构造方法；共同所属的“贪心”学习单元负责表达它们的上位关系。

“反悔贪心”处理当前选择将来可能需要撤销的情形。扫描到每个候选时先把它加入当前方案；若约束被破坏，就找出当前方案中最应撤销的一项并删除。它的正确性不能写成“发现选错了就反悔”，而应证明处理完每个前缀后，当前保留的是这个前缀内满足约束且评价最优的方案之一。

反悔贪心的基础例子使用按截止时间扫描课程或任务：暂时接受当前项目；若总耗时超过截止时间，就线性扫描当前方案并删除耗时最长的一项。这样撤销能给后续释放最多时间，同时保持当前可完成项目数量。学过 `priority_queue` 后，可以用最大堆加速“找出耗时最长项目”这一步；其他题目的撤销规则仍须重新证明，不能把“超限就弹堆顶”当成通用模板。

哈夫曼编码同样是使用小根堆的贪心算法，但它还需要前缀编码与二叉树背景，继续留在编码单元；这里只在贪心总结中建立交叉引用，不把全文搬入基础贪心单元。

## 二叉堆、priority_queue 与关联容器的定位

### 二叉树、二叉堆与优先队列的顺序

初中进阶先完成“数据结构：二叉树”单元：

1. 二叉树的结构与存储；
2. 二叉树的前序、中序与后序遍历。

随后进入独立的“数据结构：二叉堆”单元：

1. 二叉堆；
2. 堆排序（扩展）。

二叉堆与哈希表原理完成以后，进入“标准库进阶”。这一单元集中学习 `priority_queue`、四种有序关联容器和两种无序关联容器：它们不共享一种底层结构，但共同回答“怎样从标准库选择一个较复杂的现成容器”。其中 `priority_queue` 回链第 02 章的反悔贪心，说明它怎样加速反复取最值；结构原理、接口选择和算法应用仍然分篇、分单元。

二叉堆与 `priority_queue` 不并入最早的算法基础，也不塞进某一篇贪心文章。反悔贪心先建立算法思想，`priority_queue` 后来提供更高效的实现；两者仍保持独立正文和独立单元。`priority_queue` 以后还会服务哈夫曼编码、最短路、多个有序来源的合并和其他动态维护最值问题，不能把接口本身误写成反悔贪心的专用部件。

二叉树单元先区分任意二叉树与完全二叉树，再建立堆实际使用的两项性质：

- 完全二叉树除最后一层外全部填满，最后一层从左到右连续；
- 1-based 数组中，节点 `u` 的父节点是 `u / 2`，左右子节点是 `2 * u` 与 `2 * u + 1`。

二叉堆正文从这些已经理解的形状和数组编号继续推导上浮、下沉与建堆，不必临时压缩讲解一遍树。二叉树文章同时比较：完全形状允许隐式数组存储，任意形状通常需要显式保存左右孩子关系。

手写二叉堆的主要作用是理解复杂度、局部不变量和初赛知识，并为特殊堆操作打基础。普通比赛代码不要求背一份自定义堆模板；只需取最大值或最小值、加入和弹出时，最终模板优先使用 `priority_queue`。

`priority_queue` 是容器适配器，因为它在一个符合要求的底层序列容器之上只暴露 `top`、`push`、`pop` 等受限接口，并维护优先队列语义。默认底层容器是 `vector`，通常通过堆算法组织元素；“使用数组保存完全二叉树”解释默认实现为什么自然，却不是“容器适配器”这个类别名称的原因。

二叉堆单元需要读者此前已经掌握 `vector`、数组下标、复杂度、自定义比较和完全二叉树。随后标准库进阶单元把结构操作映射到标准库接口，并回访如何把反悔贪心中的线性最值查找换成最大堆或最小堆。

### 有序关联容器与哈希容器分别成组

`set`、`multiset`、`map` 和 `multimap` 是标准库进阶单元中的有序关联容器部分。它们的接口依赖键的严格顺序，天然接续排序中的比较规则与二分：

- `set` 保存互不等价的键；
- `multiset` 允许等价键重复；
- `map` 为每个互不等价的键关联一个值；
- `multimap` 允许一个等价键对应多条记录，但竞赛中频率明显较低。

核心路线要求会选择容器、插入、查找、删除、遍历、`lower_bound` 和 `upper_bound`，并理解这些操作通常是 $O(\log n)$。不要求先学习红黑树或平衡树旋转；标准只规定接口和复杂度要求，具体库实现不是使用这些容器的前置知识。

这个单元的教学价值是比较工具怎样替代前面已经理解的代码，而不是把接口变成前置捷径：

- 数据一次给完、以后只查询时，通常先排序再二分，连续存储的常数和局部性也更好；
- 数据在线插入、删除，同时还要保持顺序或反复查询边界时，有序关联容器省去手工移动元素和重新排序；
- `set` 不能按下标随机访问，重复元素、键值映射和迭代器失效规则也必须按容器语义处理；
- 学会接口不等于已经学会平衡树。以后学习平衡树时再解释旋转、维护信息和顺序统计等标准容器没有直接提供的能力。

因此有序关联容器位于初中进阶而不是算法基础。它们使用方便，但作为排序、二分或平衡树的前置知识很差；读者先拥有手写基线，才能判断什么时候值得用这个额外工具。

哈希表原理单独组成“数据结构：哈希表”单元。`unordered_set` 与 `unordered_map` 随后作为标准库进阶单元中的无序关联容器部分。它们不维护键的有序关系，平均查找、插入和删除通常是 $O(1)$，最坏情况可以退化；需要顺序、边界查询或稳定复杂度保证时不能用它们替代有序容器。

默认整数、字符串、`pair` 等类型是否已经提供哈希支持必须按 C++17 标准库实际接口说明，不能由“它能够比较”推出“它一定能够哈希”。自定义键还要保证相等判断与哈希一致：两个相等键必须产生相同哈希值。

学习路线中先保留抽象哈希表，再在同一个标准库单元中横向比较优先队列、有序关联容器和无序关联容器。这个合并只发生在课程组织层，不能把三类接口写进同一篇文章，也不能抹掉容器适配器、关联容器和哈希表之间的概念区别。

## 第 03、04 章的学习单元

这两章不按模块名平铺，而按一次能够形成完整理解的任务分组。第 03 章“初中基础”依次包含：

1. 数据结构：线性表；
2. 数据结构：栈；
3. 数据结构：队列；
4. 数学：数论基础；
5. 图论：图的概念与存储；
6. 图论：深度优先搜索；
7. 图论：广度优先搜索；
8. 图论：树；
9. 动态规划：动态规划基础；
10. 动态规划：背包；
11. 字符串：字符串基础。

“数据结构”是稳定的知识领域前缀；“线性表”才是数组与链表共同研究的上位概念。不使用“线性结构”与后面的“树状结构”制造表面对称：栈、队列、二叉树和二叉堆都属于数据结构，但它们各自维护的语义并不由几何形状决定。栈和队列分别成单元；双端队列从普通队列放宽可操作的端点，因此是队列单元的后半，不与队列并列。

“数学：数论基础”只建立后续数论共同依赖的整除、质数、算术基本定理、质因数分解、最大公约数与欧几里得算法；中国剩余定理、欧拉函数等仍在后续单元学习。图的概念与两种基础存储先形成能够表示问题的完整任务；深度优先搜索和广度优先搜索随后分别成单元。

深度优先搜索先遍历显式存储的图，再用“状态空间与隐式图”连接此前的循环枚举和后续的 DFS 枚举：部分方案是状态，一次选择是状态之间的边。子集、排列和组合枚举在这里比较候选表示、去重与生成顺序，回溯和剪枝最后总结选择、递归、撤销以及安全排除整棵子树的条件。广度优先搜索单元先建立分层遍历和最短边数语义，再用连通块练习从未访问点重新启动遍历。有根树与无根树在后续“图论：树”单元中建立术语和遍历。

动态规划基础模型先定义阶段、状态、决策与转移，再依次用数字三角形、最大子段和、最长上升子序列和最长公共子序列练习位置依赖、附加限制状态与二维状态。状态机动态规划需要额外区分同一阶段的互斥处境，移到第 05 章的常见模型。三种基础背包另以物品可用次数为主线，集中比较二维来源行和一维容量循环方向。两个单元前后相邻但各自完成一次独立任务。

第 04 章“初中进阶”依次包含：

1. 数据结构：二叉树；
2. 数据结构：二叉堆；
3. 数据结构：哈希表；
4. 标准库进阶；
5. 数学：高精度整数；
6. 图论：图搜索的应用；
7. 编码：哈夫曼编码与格雷码。

二叉堆依赖完全二叉树的数组编号，但依赖只决定相邻顺序，不足以把两个不同学习目标合成一个单元：二叉树关注左右孩子、存储与遍历，二叉堆关注堆序性质、上浮、下沉和动态维护最值。堆排序依赖同一结构但不阻塞后续，因此作为二叉堆单元的独立扩展；标准库进阶承担接口选择，并回访它能怎样加速前面已经掌握的算法。

“编码”包含哈夫曼编码与格雷码。哈夫曼编码从等长编码、前缀编码和带权路径长度出发，用二叉树、小根堆和贪心构造变长编码；格雷码仍是等长二进制编码，研究怎样让相邻编码只改变一位。二者实现方法不同，但回答的是同一个上位问题，不需要把格雷码孤立成单篇学习单元。

编码不强行归入算法基础、数据结构或数学。学习路线保留“编码：哈夫曼编码与格雷码”这个真实学习任务，模块目录则在“其他”模块下收录两篇文章；学习单元与模块目录允许给出不同但各自准确的组织方式。

高精度整数目前只有一篇主线正文，但它自身就是一次完整学习任务，不与无关主题强行合并。图搜索应用先学习多源 BFS，再用 DFS 或 BFS 染色判定二分图；它们都建立在第 03 章已经完成的图遍历上。

## 第 05、06 章的学习单元

第 05、06 章先只修正客观上混合了不同学习目标的单元，不提前预测每篇正文最终会扩展出多少内容。第 05 章已经拆开以下旧组合：单调栈、单调队列属于“数据结构：单调结构”，倍增属于“算法基础：倍增”；拓扑排序与最短路分别成单元；基环树、函数图与欧拉、哈密顿问题分别成单元。其余内容按数据结构、数学、图论、动态规划、字符串和计算几何等稳定领域加前缀。

第 06 章同样只拆明显错误组合：折半枚举与离线处理分别成单元；图的连通性与树上数据结构分别成单元。并查集扩展、状态图最短路、中国剩余定理、容斥原理、平面几何算法和动态规划进阶继续各自保持完整任务。正式名称和顺序以 `notes/learning-path.md` 为准；正文写作暴露出新的边界以后再局部调整，不把目录建设扩张成百科式穷举。

## 递归、递推、分治与动态规划的定位

这些概念共享“由较小问题得到较大问题”的外观，但处在不同层次。需要分别解释程序怎样执行、问题怎样分解、数值怎样按依赖产生，以及重复子问题怎样合并。

### 同一个递归的两种教学重点

递归仍然只有一个定义：函数直接或间接调用自身。C++ 基础中的“递归”先解释函数调用、终止条件和调用栈，这是程序能够正确执行的语言与运行机制。

算法基础再安排一篇“递归与问题分解”，不把它冒充第二种递归。它重点解释：

1. 先准确规定递归函数接收什么问题、返回什么答案；
2. 确定不再缩小也能直接回答的基本情况；
3. 把当前问题变成参数严格更小的同类问题；
4. 假设较小调用已经按函数定义返回正确结果，再组合当前答案；
5. 证明每条调用链都会到达基本情况。

这一篇位于 C++ 递归和标准库基础以后、快速排序和归并排序以前。它可以使用阶乘和朴素斐波那契递归等极小例子，但不在这里加入记忆化；朴素斐波那契产生的大量重复调用正好为以后理解动态规划留下问题。

### 递归与递推组成独立基础单元

递推关系用已经确定的较早项表示较晚项，并由初始值和依赖顺序产生整个序列。例如：

$$
f[i]=f[i-1]+f[i-2].
$$

按照下标从小到大计算斐波那契数列，只需要初始值、递推关系和计算顺序。教学上把它称为普通递推，不必为了使用了数组就提前称为动态规划。

正式学习单元安排为：

1. 递归与问题分解；
2. 递推。

二者用相同的小数列比较自顶向下调用与自底向上计算，但不重新讲一遍 C++ 调用栈，也不提前讲 DP 状态设计。一个递推关系可以用递归函数求值，也可以用循环递推；递推描述数学依赖，递归描述求值时使用的程序结构。

### 分治放在快速排序和归并排序之后

分治不是递归的同义词。它把原问题分成若干规模更小、通常彼此独立的子问题，分别求解后再合并结果；递归只是实现这些重复子问题结构的自然方式。

学习路线先具体推导快速排序和归并排序，再独立归纳“分、治、合”三步：快速排序先划分再递归，合并工作很轻；归并排序先递归再线性合并。这样读者已经看过两种不同形态，不需要在抽象定义上空转。分治单元因此紧跟排序，位于二分、序列技巧和后续搜索以前。

枚举、模拟、排序、分治、二分、前缀和、双指针和基础贪心先于第 03 章的数据结构。这样后续比较结构操作、分析复杂度和设计基线解法时，读者已经具备共同语言。

### 动态规划另设入门单元

动态规划不与递归、递推合成同一篇或同一基础单元。它建立在递推关系之上，还需要识别重复子问题，并设计足以代表子问题的信息。记忆化搜索和自底向上填表只是同一状态依赖的两种求值顺序。

动态规划中常见的“阶段、状态、决策”应当准确解释：

- 阶段表示问题向前推进的层次或顺序，例如处理到第 `i` 个元素、走到第 `i` 个位置；
- 状态是在某个阶段足以唯一描述当前子问题、并决定后续结果所需的信息；阶段编号经常就是状态的一个维度，甚至是唯一维度，不能把阶段强行排除在状态之外；
- 决策是当前状态允许采取的选择，例如选或不选、最后一步走 $1$ 级还是 $2$ 级；
- 状态转移描述一个状态经过某个决策会依赖或到达哪个状态，转移方程再对全部合法决策对应的结果进行求和、取最值或逻辑合并。决策本身不等于状态转移方程。

`dp[i]` 的含义必须按问题精确定义。它可以表示“只考虑前 `i` 个元素的答案”“恰好以 `i` 结尾的答案”或“规模恰好为 `i` 的子问题答案”，不能一律写成含糊的“规模不超过 `i` 的问题”。

动态规划也不只处理最优化决策。计数、可行性和概率问题同样可以形成 DP；在爬楼梯计数中，把所有走法按最后走 $1$ 级或 $2$ 级分类，就是两种决策对应的前驱状态。

入门单元建议保留：

1. 动态规划：阶段、状态、决策与转移；
2. 动态规划：线性 DP。

现有 `dp-state-transition.md` 保持稳定文件身份，但下一次修订需要补齐上述四个概念，并从暴力递归的重复子问题推导记忆化与递推。难度继续分层：

1. 动态规划入门：阶段、状态、决策、转移和基础线性 DP；
2. 初中阶段主干：最长上升子序列、0-1 背包、完全背包和多重背包；
3. 后续类型：区间 DP、DAG 上的 DP、树形 DP、状态压缩 DP、数位 DP；
4. 扩展与优化：单调队列、斜率、分治、四边形不等式等优化方法。

## 下一步

当前按以下顺序逐项落实：

1. 提交第 01–06 章本轮学习单元、命名与扩展规则重组，让正式路线、模块目录、母规范和本文一致。
2. 回到正式学习路线，从第 03 章当前进度向后逐篇审计；已有正文达到当前标准时直接跳过，不为制造改动重新写一遍。
3. 需要修订或新建的文章逐篇补齐正文与小测，并在每篇完成后独立检查、独立提交。
4. 第 03–06 章核心正文完成并通过主线审计后，再按扩展阅读索引和父文章链接补齐扩展。
5. 每完成一个真实学习单元，再根据实际正文检查边界；允许局部拆分、合并或移动，但不暂停内容写作等待所谓完美大纲。
