export function IndexingConvention() {
  return (
    <aside className="route-convention" aria-label="下标与区间约定">
      <strong>下标与区间约定</strong>
      <p>
        本书自定义的对象默认从 1 开始编号并使用闭区间；讲解 C++ 与 STL 接口时，保留其原生的从 0 开始编号和左闭右开区间。
      </p>
    </aside>
  );
}
