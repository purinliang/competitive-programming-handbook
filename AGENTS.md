# AGENTS.md

本文件约束 AI agent 在本仓库中的工作方式。默认使用中文沟通，除非用户明确要求其他语言。

开始工作前先阅读 `README.md`。仓库定位、目录职责、C++ 风格和模板设计原则以 `README.md` 为准。

## 模板维护

- 模板库追求竞赛实用性，不为了覆盖少数变体而做复杂抽象。
- 写模板时优先保证常见形态简洁、完整、好改。
- 对外部模板先提取算法不变量和每段代码的作用，再按本仓库稳定的命名、下标和接口重写；不保留只能靠背诵、无法从原理恢复的写法。默认优先可理解、可验证和可修改性，允许为此放弃少量常数性能；只有题目约束证明常数成为瓶颈时，才换用更难读的优化实现。
- 不使用 `#define` 压缩常量、类型、左右儿子或递归参数；常量使用 `const`，类型别名使用 `typedef`，函数参数和算式显式写出。
- 写验证入口时允许保留题目特有适配代码和胶水代码，并用注释说明相对模板的特殊之处。
- 如果模板命名、注释或写法更新，同步检查并更新相关验证入口。
- 当多个题目暴露出稳定共性时，再把共性抽回模板。

## C++ 命名

- 本仓库优先维护作者已经长期使用的简单、统一命名体系，不以“另一种写法也很常见”为由擅自替换。新模板先从已定稿代码归纳命名；无法归纳时保留待审核写法，不自行引入新缩写或风格。
- 代码中按照既有约定使用 `int` 和 `ll`；正文讨论 C++ 类型、声明或接口时写正式类型名 `int`、`long long`，讨论数值容量、数据范围或算法在某个位宽下的表现时写“32 位整数”“64 位整数”，不写“`ll` 范围”或“`long long` 范围”。只有当符号性本身影响结论时，才特意写“有符号”或“无符号”。
- 固定模数的模整数类型统一命名为 `modint`，竞赛代码中的短别名统一写作 `typedef modint mint;`；模数优先写作全局 `const ll MOD`，不在类内保存可变的共享模数。正文和模板不再交替使用 `ModInt` 等其他拼写。
- 教程和模板自己定义的题目对象、序列与算法状态默认使用 1-based 下标、闭区间和 `+5` 容量余量；这一语义不因底层使用静态数组还是 `vector` 而改变。只有直接讲解或调用 C++ / STL 原生接口时，才保留内置数组、`string`、`vector` 的 0-based 下标与迭代器左闭右开区间；两套表示只在接口边界集中转换。完整规则见 `notes/AGENTS.md` 和 `notes/learning-path.md`。
- 函数名与参数列表视为一个整体，函数声明、定义和调用的左括号前不加空格，写作 `void dfs(int u, int p)` 和 `dfs(v, u)`。`if`、`for`、`while` 和 `switch` 等控制关键字与左括号之间保留一个空格。`.clang-format` 使用 `SpaceBeforeParens: ControlStatements`，教程代码块也遵守同一规则。
- C++ 代码每行以 `80` 列为上限，由 `.clang-format` 的 `ColumnLimit` 统一约束；模板和 Markdown 中的 C++ 代码块使用同一配置，不靠人工维护两套换行规则。
- 图、树和数组模拟树中，当前点或当前节点统一命名为 `u`；与它相邻或由它转移到的点统一命名为 `v`。
- 一次性竞赛题解按照“题目级共享状态”和“当前过程状态”划分名称。全局唯一并且会跨函数维护的输入、图和结果，例如 `n`、`a`、`prefix`、`g`、`dist`、`par`、`dep` 和 `siz`，声明为全局动态容器或全局变量，并在读入规模后按 `n + 5` 分配；即使当前基础示例只有 Dijkstra 等一个算法函数使用这些量，也按加入题目特化后仍然成立的结构书写，不把整份共享状态塞进参数列表。只描述当前递归、遍历或区间操作的 `u`、`v`、`p`、`l`、`r`、`mid` 等保留为局部变量或函数参数，因此典型接口写作 `dfs(int u)`、`dfs(int u, int p)` 和 `bfs(int s)`。需要构造多个实例、长期维护对象内部不变量或绑定一组操作的数据结构使用 `struct` 封装，其持久状态属于成员，当前操作信息仍通过参数传入；没有题目级共享对象的数论等纯函数、验证工具和通用库接口可以按语义显式传参。
- 图论和树上题解的稳定全局缩写统一为：距离 `dist`、深度 `dep`、度数 `deg`、子树或当前集合大小 `siz`、父节点数组 `par`、访问标记 `vis`；颜色保留 `color`，不缩成含义过多的 `c`，也不用通常表示 column 的 `col`。源点或起点使用 `s`，终点、目标点或汇点使用 `t`；局部父节点继续使用 `p`。不用中文 OI 特有的 `fa`，不用较少见的 `pa`，也不用会在 `using namespace std;` 下与 `std::distance` 发生名称歧义的全局 `distance`。倍增祖先表等语义不同的状态可以使用 `up`，不为了表面统一强行改成 `par`。这些缩写只用于含义已经稳定的竞赛状态，不把任意长名称随意截短。
- 题目级动态数组、邻接表和数据结构内部存储默认使用 `vector`，按照真实输入规模分配并保留 `+5` 余量；这避免固定 `MAXN` 估错，但 `operator[]` 仍不检查越界。`solve`、`init` 或其他初始化入口必须允许以后再次调用：读入规模后统一使用 `assign(n + 5, value)` 同时确定长度并重置全部元素，零值也显式写作 `assign(n + 5, 0)`，邻接表写作 `g.assign(n + 5, {})`。只有算法明确要保留原有前缀并扩大或缩小序列时才使用 `resize`；只有容器尺寸已经确定并且需要重填现有区间时才使用 `fill`。邻接表使用 `vector<vector<int>> g`，不用固定外层容量的 `vector<int> g[MAXN]`；一般稀疏图不用占用 $O(n^2)$ 空间的邻接矩阵代替邻接表。
- 有根树遍历中，当前节点的父节点命名为 `p`，典型接口写作 `dfs(int u, int p)`。遍历 `g[u]` 的邻居 `v` 时，跳过父节点统一写作 `if (v == p) continue;`：当前被检查的邻居 `v` 是条件的主体，父节点 `p` 是用来比较的对象。不交换为 `p == v`。
- 线段树等数组模拟树的当前节点仍使用 `u`，左右儿子分别是 `u * 2` 和 `u * 2 + 1`；不用 `p` 表示当前节点，以便把 `p` 稳定保留给父节点。
- 表示数组下标而非树节点的 `x`、`pos` 等名称不受上述规则影响。树状数组的跳转下标统一使用 `x`，普通顺序遍历的循环变量继续使用 `i`。

## 教程维护

编辑 `notes/` 下的教程、目录或长期图片资产前，必须同时阅读并遵守 `notes/AGENTS.md`。

## Git 规范

这是个人仓库，默认不使用复杂分支模型。除非用户明确要求，直接在当前分支上做小步提交。

- 提交信息保留 `notes:`、`tools:` 等英文类型前缀，冒号后的说明使用中文并准确概括改动。

提交信息使用根目录或仓库区域作为前缀，后接简短中文标题：

```text
<area>: <中文标题>
```

常用 `area`：

- `notes`：知识笔记、复盘、教学材料
- `templates`：模板代码和验证入口
- `tools`：图示工具、生成器、脚本
- `workspace`：工作区样例或临时流程
- `repo`：仓库结构、README、AGENTS、配置

如果一次提交横跨多个区域，优先拆分提交；确实无法拆分时使用 `repo`。

提交规则：

- 标题尽量短，只说明核心变化。
- 较大的修改优先拆成多个提交。
- 如果一个提交确实需要说明多点变化，在标题后空一行，再用无序列表说明。
- 提交代码前必须运行相关格式化或检查；涉及 C++ 文件时，至少运行 `clang-format` 或 `make format <path>`。

推荐格式：

```text
templates: 增加基础代码框架

- 增加 C++17 竞赛入口结构
- 增加本地调试开关约定

notes: 整理前缀和笔记
repo: 初始化仓库说明
```

提交前必须检查 `git status`，不要混入无关改动。除非用户明确要求，不要提交或执行 destructive Git 操作。
