[
  {
    "id": "boolean-values",
    "prompt": "C++ 的 `bool` 可以保存哪两个值？",
    "options": [
      { "id": "a", "text": "`yes` 和 `no`" },
      { "id": "b", "text": "`true` 和 `false`" },
      { "id": "c", "text": "`positive` 和 `negative`" },
      { "id": "d", "text": "任意两个整数" }
    ],
    "correctOptionId": "b",
    "explanation": "`bool` 专门表示二选一状态，只有布尔字面量 `true` 和 `false` 两种取值。"
  },
  {
    "id": "choose-boolean-state",
    "prompt": "下面哪个信息最适合使用 `bool` 保存？",
    "options": [
      { "id": "a", "text": "一个点是否访问过" },
      { "id": "b", "text": "一个点被访问了多少次" },
      { "id": "c", "text": "一条路径的总长度" },
      { "id": "d", "text": "一个人的姓名" }
    ],
    "correctOptionId": "a",
    "explanation": "是否访问过只有真假两种状态；次数和长度需要整数，姓名则需要字符串。"
  },
  {
    "id": "comparison-result",
    "prompt": "表达式 `a == b` 的结果是什么类型？",
    "options": [
      { "id": "a", "text": "`char`" },
      { "id": "b", "text": "`int`" },
      { "id": "c", "text": "`bool`" },
      { "id": "d", "text": "`double`" }
    ],
    "correctOptionId": "c",
    "explanation": "相等关系要么成立、要么不成立，因此比较表达式产生 `true` 或 `false`，类型是 `bool`。"
  },
  {
    "id": "integer-to-boolean",
    "prompt": "把整数 `-7` 转换成 `bool`，结果是什么？",
    "options": [
      { "id": "a", "text": "`false`，因为它小于零" },
      { "id": "b", "text": "`true`，因为它不是零" },
      { "id": "c", "text": "`-1`" },
      { "id": "d", "text": "发生编译错误" }
    ],
    "correctOptionId": "b",
    "explanation": "整数转换成 `bool` 时，只有零变成 `false`；所有非零整数，包括负数，都变成 `true`。"
  },
  {
    "id": "default-boolean-output",
    "prompt": "没有使用 `boolalpha` 时，`cout` 默认怎样输出 `false` 和 `true`？",
    "options": [
      { "id": "a", "text": "`false true`" },
      { "id": "b", "text": "`no yes`" },
      { "id": "c", "text": "`-1 1`" },
      { "id": "d", "text": "`0 1`" }
    ],
    "correctOptionId": "d",
    "explanation": "默认布尔输出使用整数形式：`false` 输出为 `0`，`true` 输出为 `1`。"
  },
  {
    "id": "assignment-vs-equality",
    "prompt": "若要判断 `a` 与 `b` 是否相等，应使用哪段表达式？",
    "options": [
      { "id": "a", "text": "`a = b`" },
      { "id": "b", "text": "`a == b`" },
      { "id": "c", "text": "`a := b`" },
      { "id": "d", "text": "`a === b`" }
    ],
    "correctOptionId": "b",
    "explanation": "`==` 判断两个值是否相等；单个 `=` 是赋值，会修改左侧变量。"
  },
  {
    "id": "boolean-storage-size",
    "prompt": "主流竞赛环境中 `sizeof(bool)` 通常是 `1`。这能推出什么？",
    "options": [
      { "id": "a", "text": "`bool` 能保存 256 个不同整数" },
      { "id": "b", "text": "`bool` 与 `char` 是同一个类型" },
      { "id": "c", "text": "`bool` 可以写成 `unsigned bool`" },
      { "id": "d", "text": "对象通常占一个字节，但仍只有 `false` 和 `true` 两种值" }
    ],
    "correctOptionId": "d",
    "explanation": "存储大小与类型允许的取值数量不是同一个概念；`bool` 通常占一个字节，但逻辑值仍只有两种。"
  },
  {
    "id": "double-logical-negation",
    "prompt": "整数 `x` 为 `0`、`7` 和 `-7` 时，表达式 `!!x` 依次得到什么？",
    "options": [
      { "id": "a", "text": "`true`、`false`、`false`" },
      { "id": "b", "text": "`false`、`false`、`true`" },
      { "id": "c", "text": "`false`、`true`、`true`" },
      { "id": "d", "text": "`0`、`7`、`-7`，完全不变" }
    ],
    "correctOptionId": "c",
    "explanation": "第一次 `!` 取反整数对应的真假，第二次再取反回来，因此 `!!x` 等价于把零变成 `false`、非零变成 `true`。"
  }
]
